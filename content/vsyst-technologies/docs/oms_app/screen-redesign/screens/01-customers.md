# Screen spec — `Customers` (dealer)

> Screen slug `customers` · route name `Customers` (unchanged) · folder `src/screens/v2/Dealer/Customers/` · registry key `Dealer/Customers` · toggle `screen_v2_dealer_customers` · size **S** (one read model + one preference command; the filter sheet gets its own sub-spec `01b`) — see [[../03-per-screen-playbook]] and [[../templates/screen-spec]].
>
> **Status line:** Spec draft 2026-09-03 (eight rounds) → **Spec agreed 2026-09-03** (user; O‑2 deferred to sub-spec `01b`) → **O‑2 closed 2026-09-04** ([[01b-customers-filter-sheet|01b]] session: newest sales order, all time) → **"start API for customers" 2026-09-04** → **API red → API green 2026-09-04 (PR #40 → `slave`, stacked on #39)** → Design session held 2026-09-04 on the simulator build (three rounds on the design canvas, decisions 42–50 in §4) → **"start" design restyle 2026-09-04 (app PR stacked on #50)** → **Design corrections 2026-09-06 (decisions 51–58) → Final v2 red → green the same day on PR #51, sort menu as pictured (decisions 59–60) — 40 suites / 837 tests, CI green** → **credit sheet Option F red → green 2026-09-06 (decisions 61–64; `3e9e7cf7` → `c425b8c1`, unlimited gradient bar `c8b03c25` → `ac8bd87a`; 40 suites / 838 tests, CI green; `AI.md` now makes test-first the contract for every agent, `2945e2ff`)** → **iOS device run 2026-09-06 → decisions 65–68 + F-APP-7 type scale red → green 2026-09-06/07 on PR #51** (WP-A `9d5bfb2a` + A2 `3ff37a35` list/keyboard; WP-B `48c7aa00` foundation; WP-D `011be469` + D2 `8648dbd2` sheet; WP-C `3b6f81d2` + C3 `312996ef` chrome/row; head `d77c2106`, 49 suites / 984 tests, CI green; scroll-to-top, keyboard, the sheet at 1 / 1.353 / 2.143 and the chrome at 2.143 verified on the simulator) → **mechanism 6 (a text-size change remounts the screen) C4 `1bf9dce6`, merged `dcee66ec`** → **F-APP-9 orientation + F-APP-10 language planned, shown, approved ("no packages now") and built 2026-09-07**: O1 `a7ecb372`, L1 `90e376cf`, O2 `b207162b` (decision 70), L2 `af56f8eb` (decision 69), merged through `41cddd7c` (64 suites / 1241 tests, pushed); verified on the simulator: v1 stays portrait while Customers rotates, the contained column with chips + card on one line in landscape, Hindi at 1× and 2.143×; the header's Hindi title fix in flight · not merged → **decision 71 (what a tap lands on) red → green 2026-09-08: `3d326552` → `62565a0e`, merged `b010a8e8`, then the midpoint amendment T1b `9f032d70` → `51c074d3` (merged `684fcce5`), docs `5dd24f97` (merged `5a6542d8`) — 64 suites / 1259 tests, pushed; verified on the simulator 2026-09-08: five taps (both halves of the blank under a name, the row's top edge, both halves of the tile gap) land as designed, nothing moved at 1× or 2.143×** → **decision 72 (the filter chip names its state; glyphs and the error fill gone) red → green 2026-09-08: `6f6ef9d2` → `3e41f0fe`, merged `e7472c5f` — 64 suites / 1264 tests, pushed; verified on the simulator in English and Hindi: Unverified / No Trans. · असत्यापित / बिना लेन-देन filled with no glyph, 5 / 15, reset to the outline chips; docs `e18cea8a` merged `1e390041`** → **decision 72 amended (washes + fixed width via the ghost word) red → green 2026-09-08: `cb4ce91b` → `73f1f15b`, merged `f7fc5754` — 64 suites / 1267 tests, pushed; verified on the iPhone 17 Pro Max simulator: primary wash / error wash / outline, the Has Trans. chip does not move across the three taps, in English and in Hindi (the wider word wins per language); docs `824f951b` merged `5c78282e`** → **F-APP-10 mechanism 7 (Android 15+ text width) 2026-09-08 evening: T4 flag `2353ad1b` → `53a5bf5d` merged `50188ef4` CRASHED at launch; fix `97f7fcd6` → `fe8799d5` (theme `useBoundsForWidth=false`, Kotlin back to the template) merged `73bcea62` — 65 suites / 1273 tests, pushed; verified on the Pixel 10 Pro Fold emulator at 320 / 390 / 420 / 480 dpi in Hindi; Bold Text (Android) clips app-wide — open, RN-level; decision 73 (sort row when the chips wrap) red → green `21636845` → `65f6ef9e` + docs `f7fd87c8`, merged `77b70a15` — 65 suites / 1279 tests, pushed; verified on the Pro Max at 2.143 (portrait full-width row, landscape column, back); two `flexBasis` cuts failed on the device, `width` of the measured row holds; drawer-flash-on-rotation device entry (found by a parallel run, deferred) committed `b7bc6782`** → **Design red → Design green 2026-09-04 (app PR #51 `app_screen_customers_design` → `slave`, stacked on #50; 836 tests, CI green, ready for review, not merged)** → **"start screen customers" 2026-09-04** → **Screen red → Screen green 2026-09-04 (app PR #50 → `slave`, stacked on #49; 17 decision tests, suite 771, gate PASS)** → Shipped in app vX.Y (after the user merges #39 → #27 → #49 → #40 → #50 and runs the device checklist in `docs/screens/customers-v2.md`) → Old screen removed (PR #) → **Shapes build (decisions 74–76: ladder, keyboard inset, panel) red → green 2026-09-13 on the same branch, uncommitted — 70 suites / 1521 tests; device run on the 17 Pro Max simulator + Pixel 10 Pro Fold AVD the same day (see [[01c-customers-shapes]] status and `docs/screens/customers-v2.md`)** → **Reviewed 2026-09-23** ("also code-review PR #52 and #51 use agent teams"): head `d0fbd081`, 71 / 1,588 green; **four defects still live at the top of the stack** — the UNLIMITED bar the wrong width after a rotation (v1's `ProgressSVG` freezes `Dimensions.get` at launch), the layout's boxes measured with the Latin line height under Hindi, the sort button announcing its raw key, the sheets' open state stranded by a `Screen` remount — two for a device (the keyboard paid twice on Android ≤ 14, the per-app language picker not honoured), and Settings' Language row in literals. Three fixed and two reduced by #54; none by #52 or #53; nine open at `dcde2c8b`. → [[reviews/pr-51-code-review-2026-09-23]]. Nothing applied.
>
> **Next gate:** Phases 0–2 built 2026-09-04 (API PR #39, dip-web PR #27, app PR #49 — awaiting the user's merges) and the `01b` session held the same day. This screen is fully specified; nothing in either repo changes for it before **"start API for customers"**.
>
> Facts without a marker were read from the code on 2026-09-03 (paths inline). Former assumptions **A‑1…A‑8** are all decided (see §4) and **O‑2 was closed on 2026-09-04** in [[01b-customers-filter-sheet]] (decision 36). No open definitions remain. From here on, any change to §2, §3 or §5 follows [[../01-tdd-workflow#1.4 How discussion sessions feed the tests|01 §1.4]]: spec first, then the test, then the code, in one PR.

## 1. Purpose ⛔

- **One sentence:** a dealer user sees every customer company the dealer serves — today's orders, account balance and credit utilisation per customer, plus the totals for the customers currently shown — and jumps from a row to that customer's settings, daily summary, ledger or credit summary.
- **Replaces:**
  - `src/screens/Dealer/Customers/index.js` (list, client-side search/sort/filter, on-focus fetch, header refresh hack `navigation.replace('Customers')`)
  - `src/screens/Dealer/Customers/RelationFilterBS.js` (filter/sort bottom sheet — its v2 replacement is planned in the sub-spec `01b-customers-filter-sheet.md`, session to be scheduled)
  - `src/components/Balance/Components.js` (`Card_balance_helpers` row) — **correction 2026-09-04 (docs agent, re-grepped):** its only importer on the branch is the v1 Customers screen; `Customer/Dealers` uses its own local `./components`. So it can be deleted with the v1 screen at Step 6 — re-grep before deciding
  - the dealer-list use of `src/screens/Common/RelationList/RelationCreditBS.js` (still imported by `CustSettings`, `Customer/Dealers`, `DealerSettings`, `Customer/NewOrder` — **not** deleted by this screen)
  - Not touched: `CustSettings.js` (1,962 lines), `Discount.js`, `SetDiscBS.js`, `Common/DailySummary`, `Common/Accounts`, `Common/AdvDepLedger` — they stay on v3 and remain the exit points.
- **Roles:** dealer only; superadmin out of scope (D5). Decided 2026-09-03:
  - list, summary strip, tiles and donut: **every** dealer scope (`DPrimary`, `DAdmin`, `DOrder`, `DAccount`, `DOrderAccount`, `DView`) — "everyone can view balances"
  - Settings strip and avatar tap → `CustSettings`: `DPrimary`, `DAdmin` only (as v1, `index.js:333`)
  - credit % label and the credit sheet: only when the user's company entry has `amend_prem.allow ∋ "credit.read"` (as v1, `Components.js:38`; default for `DPrimary`/`DAdmin` per `api_v3/services/users.js:60`, editable per user from the Users screen)
- **No "Add Customer" flow on the dealer side** (decided 2026-09-03, twice). The "Add Dealer" button in the shared frame belongs to the customer-side Dealers screen, which is a future screen.
- **Entry points:** Drawer → "Customers" (`navigation.navigate('dealerCustomer', { screen: 'Customers' })`, `DrawerContent.js:487`). Header: menu left, title "Customers", refresh icon right (kept from the frame; v2 refetches instead of re-mounting).
- **Exit points (params exactly as v1 sends them, because the target screens stay v3):**
  - `CustSettings` `{ dealer_custID: { dealer_id, cust_id } }` — also the **only** place a customer is blacklisted or un-blacklisted (A‑8)
  - `DailySummary` `{ companyID: cust_id, companyName, ID_FIELD: 'cust_id' }`
  - `Accounts` `{ companyID, companyName, companyAdvance: adv_dep, ID_FIELD: 'cust_id', previous_screen: 'Customers' }`
  - v2 credit sheet (in-screen, rendered from the tapped row — no navigation, no request)
  - v2 filter sheet (in-screen; sub-spec `01b`)

## 2. What the user sees ⛔

Layout mirrors the customer-side "Dealers" frame shared 2026-09-03 (see §4) with the dealer-side substitutions: title "Customers", search placeholder "Search Customer name" (as the filter frame shows), **no Add button**, avatar = customer image/initials instead of the oil-company logo, summary label "Customer". **Row = the Dealers frame's row** (donut + %, no numbered badge) — decided round 7.

### 2a. List screen

| #   | Element                             | Data (source collection · field)                                                                                                                                                          | Derived? (formula)                                                                                                                                                                                                                                                                                                                                                                 | Role-visible                             | Loading / empty / error state |
| --- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------- |
| 1   | Search field                        | client state → body `q`                                                                                                                                                                   | debounced 300 ms; page reset in the same commit as the text change; list scrolls to top on a new search (the `SelectVehicle.js` pattern)                                                                                                                                                                                                                                           | all                                      | —                             |
| 2   | Filter button + count badge         | client state (`sort`, `dir`, `filters`)                                                                                                                                                   | `activeFilterCount({ sort, dir, filters })` = number of settings that differ from the defaults (`createdAt` / `desc` / no filters) — new Tier 1 helper; badge hidden at 0                                                                                                                                                                                                          | all                                      | —                             |
| 3   | Summary · "Customer m / n"          | server `summary.matching` / `summary.all`                                                                                                                                                 | **two numbers** (round 6): `matching` = relations passing the current `q` + `filters`; `all` = every relation of the dealer, blacklisted included                                                                                                                                                                                                                                  | all                                      | skeleton                      |
| 4   | Summary · "Daily total"             | server `summary.dailyTotal`                                                                                                                                                               | Σ of `today.amount` over the **matching** relations — all of them, not just the pages loaded so far; follows filter and search — decided round 5                                                                                                                                                                                                                                   | all                                      | skeleton                      |
| 5   | Summary · "Total Balance"           | server `summary.totalBalance`                                                                                                                                                             | Σ of `credit.boip` over the **matching** relations — same universe as row 4 — decided round 5                                                                                                                                                                                                                                                                                      | all                                      | skeleton                      |
| 6   | Row · tags `CR` `DR` `OTP`          | `dealer_custs.cs_reimb` · `dealer_custs.products[]` × `prod_msts.categories[]` · `dealer_custs.dvr_otp`                                                                                   | server booleans: `tags.cr` = `cs_reimb`; `tags.dr` = the relation has a `products[]` entry with `disc_type !== "none"` and `disc_val > 0` whose `dealer_prod_id` is a dealer product whose PRIMARY category is **Diesel** (`prod_msts.categories ∋ { group_name: "PRIMARY", name: "Diesel" }` — the same rule `so_msts.js:210-213` uses to stamp `p_ctgy`); `tags.otp` = `dvr_otp` | all                                      | —                             |
| 7   | Row · avatar                        | `cust_msts.cust_img`, `cust_msts.cust_name`                                                                                                                                               | image when `img` present, else `initials(name)` (first letter of the first two words, upper-case — the filter frame shows "CO", "CT") — new Tier 1 helper — decided round 5 (A‑2)                                                                                                                                                                                                  | all                                      | —                             |
| 8   | Row · unverified dimming            | `dealer_custs.dealer_verified`                                                                                                                                                            | `opacity 0.6` when `false` (as v1)                                                                                                                                                                                                                                                                                                                                                 | all                                      | —                             |
| 9   | Row · "Settings" strip under avatar | user scope (auth store)                                                                                                                                                                   | `canOpenSettings(scope)` = scope ∈ {DPrimary, DAdmin} — new Tier 1 helper                                                                                                                                                                                                                                                                                                          | DPrimary, DAdmin                         | —                             |
| 10  | Row · customer name                 | `cust_msts.cust_name`                                                                                                                                                                     | 1 line, tail ellipsis                                                                                                                                                                                                                                                                                                                                                              | all                                      | —                             |
| 11  | Row · daily tile (green)            | `so_msts` of the relation with `on_dt` inside the **IST calendar day** of the request: `Σ ptotal + Σ cs_reimb` (`getPtSum`/`getCsSum`)                                                    | server `today.amount` (2 dp); window computed on the server (decided 2026-09-03), no dates in the body. Label copy ("Daily Total" in the filter frame, "Daily Orders" in v1) is a design-session item                                                                                                                                                                              | all                                      | `₹ 0.00` when none            |
| 12  | Row · balance tile (blue)           | `dealer_custs.cust_bal` FY opening (`yearBalance`) + `month_crdrs` FY cumulative (the `calcFYCumulativeBal` rule)                                                                         | server `credit.boip` (2 dp); ledger rule lives in `api_v3/services/ledger_window.js` — v4 composes, never copies                                                                                                                                                                                                                                                                   | all                                      | skeleton                      |
| 13  | Row · credit donut + % label        | `dealer_custs.max_cr_lmt`, `dealer_custs.adv_dep`, `credit.boip`, `credit.pendingPOs` (`order_msts` PENDING/PROCESSING), `credit.uninvoicedSOs` (`so_msts` without `inv_id`/`gst_inv_id`) | `creditUtilization({ maxCrLmt, adv_dep, maxOut: boip + pendingPOs + uninvoicedSOs })` — exists and is tested in `src/helpers/Credit/`; UNLIMITED → flat bar; ratio > 0.85 → error colour (frame: 90 % red, 85 % and 65 % primary)                                                                                                                                                  | donut: all · % label: `credit.read` only | —                             |
| 14  | Row · "Blacklisted" chip            | `dealer_custs.hidden` — the **Blacklist** switch in Customer Settings (`CustSettings.js:1816-1824`)                                                                                       | shown when `blacklisted` is true; row dimmed like unverified; blacklisted customers stay in the list whenever they pass the filter — **decided round 7 (A‑8)**                                                                                                                                                                                                                     | all                                      | —                             |
| 15  | List end                            | `page.hasMore`                                                                                                                                                                            | footer spinner while the next page loads; nothing when `hasMore` is false                                                                                                                                                                                                                                                                                                          | all                                      | —                             |
| 16  | List empty                          | —                                                                                                                                                                                         | "No customers" (no filter) / "No customers match" (filter or search active) — strings in `strings.js`                                                                                                                                                                                                                                                                              | all                                      | illustration + retry          |
| 17  | List loading (first page)           | —                                                                                                                                                                                         | row skeletons ×12                                                                                                                                                                                                                                                                                                                                                                  | all                                      | —                             |
| 18  | Screen error                        | `errorRTK` message                                                                                                                                                                        | —                                                                                                                                                                                                                                                                                                                                                                                  | all                                      | full-screen error with retry  |
| 19  | Offline                             | —                                                                                                                                                                                         | design session decides (v1 has nothing)                                                                                                                                                                                                                                                                                                                                            | all                                      | banner, last data stays       |

**2026-09-04 design-session amendments to this table (decisions 42–50 in §4; the row numbers above keep their meaning, the rendering changes):**

- Rows **3–5**: the summary strip is a **header card** above the list with the three labels ("Customers" · "Today" · "Total balance") sitting on its top border like a fieldset caption and the values inside; its Today / Total-balance columns share the row's tile flex weights (136 / 145) and left offset (avatar column + gap), so they sit exactly over the tiles. Total balance is the hero (titleLarge, `link`); today `success`; count `primary`.
- Row **2** gains an **applied-filter chip row** under the search: one chip per boolean status filter (✓ only / ✕ exclude) and one for a non-default sort with its direction arrow; `appliedChips(applied)` (Tier 1) mirrors `activeFilterCount`; tapping any chip opens the filter sheet; the badge stays. Search is a 44 pt pill; the filter button a 44 pt round tinted button.
- Row **6**: tags `CR · DR · OTP` are plain primary text **under the avatar**, bullet-separated, no chip background, at a **9 pt caption size** (`captionTiny` token, not an MD3 role — user follow-up 2026-09-04: "they are just to show, no action is required of them").
- Row **7**: avatar is a **44 pt circle** (× OS font scale) that **wears the credit ring** (row 13).
- Row **8**: an unverified row is dimmed **and** carries a small grey **"Unverified"** chip under the name.
- Row **9**: the Settings strip is gone; **a right chevron ends the name line** and the name row is the target (same `canOpenSettings` rule; no chevron, no tap otherwise).
- Rows **11–12**: the two tiles show the **amount only** (no labels — the header card is the legend), 32 pt tall plus hit slop, tinted `success` / `link`, radius md.
- Row **13**: the ring wraps the avatar; **no % on the row**; error colour over 0.85, primary under; **UNLIMITED = dotted hairline ring (supersedes the flat bar)**; BLOCKED = full error ring; the ring-avatar is the credit-sheet target under `credit.read` **in every state, UNLIMITED included (user, 2026-09-04 follow-up: "as it did before" — v1 opened the sheet for an unlimited line too; the sheet shows "Unlimited" plus deposits and dues)**; without the permission it renders and does not open.
- Row **14**: "Blacklisted" is a small error-tint chip under the name.
- Layout: the list is a white sheet (radius lg top corners) with hairlines inset to the text column; the screen passes `edges={[]}` to `Screen` and puts `insets.bottom` in the list's content container (decision 42).

### 2b. Credit sheet (opens on donut tap, `credit.read` only)

Decided 2026-09-03 (round 4): **rendered from the tapped row's `credit` object — no request on open, no refresh button.** The list's refresh paths (screen focus, pull-to-refresh, header refresh icon, return from a settings edit) are the sheet's refresh paths, so the sheet always agrees with the donut it opened from. Every element below is pure presentation of row data through `creditSummary(credit)` (Tier 1).

| #   | Element                         | Data                             | Derived? (formula)                                                                                                        | Loading / error                                                               |
| --- | ------------------------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 20  | Title "Credit Summary" + name   | row `cust.name`                  | —                                                                                                                         | —                                                                             |
| 21  | Utilisation bar + % + available | row `credit`                     | `creditUtilization(...)`; available = `pool − maxOut`; UNLIMITED → flat bar and no available                              | —                                                                             |
| 22  | Advanced Deposits               | `credit.adv_dep`                 | —                                                                                                                         | —                                                                             |
| 23  | Credit Limit by Dealer          | `credit.max_cr_lmt`              | hidden when UNLIMITED; "0" when BLOCKED (as v1)                                                                           | —                                                                             |
| 24  | Maximum Credit Limit            | —                                | `max_cr_lmt + adv_dep` (hidden when UNLIMITED)                                                                            | —                                                                             |
| 25  | Unpaid Invoices                 | `credit.boip`                    | —                                                                                                                         | —                                                                             |
| 26  | Unbilled Sales Orders           | `credit.uninvoicedSOs`           | —                                                                                                                         | —                                                                             |
| 27  | Pending Orders                  | `credit.pendingPOs`              | —                                                                                                                         | —                                                                             |
| 28  | Total Credit used               | —                                | `boip + uninvoicedSOs + pendingPOs`                                                                                       | —                                                                             |
| 29  | Balance Credit                  | —                                | `pool − maxOut` (hidden when UNLIMITED)                                                                                   | —                                                                             |
| 30  | "Updated HH:MM" footer          | list response `meta.generatedAt` | `formatUpdatedAt(iso)` → IST time of the list load (new Tier 1 helper) — decided round 5 (A‑7), replaces a refresh button | shown always; the design session decides whether ages > 5 min are highlighted |

**2026-09-06 design-session amendments to the credit sheet (decisions 61–64; six options A–F drawn on the canvas page "Credit sheet", user picked F and edited it there):**

- **61. Option F layout.** Header: `Close` on the left in the **error** colour, "Credit Summary" centred, hairline under it. Then a **summary card** — a plain 44 pt avatar circle (no ring), the name, and a 44 / 4 **ring carrying the percentage** on the right (error over 0.85, primary under); a full-width bar; and one line with the quiet `used / pool` figure on the LEFT and `₹ … available` in `success` on the RIGHT. Then **two 50 % panels** side by side — **Credit line** (success tint) on the left with Advance deposits, Credit limit by dealer and the **Maximum credit limit** total; **Used** (error tint) on the right with Unpaid invoices, Unbilled sales orders, Pending orders and the **Total credit used** total. Everything inside a panel is **flush right** (the panels are too narrow for label-and-number on one line), each row a quiet `labelSmall placeholder` label over its number; the panel header is the uppercase title over a **neutral** hairline (no bullet, no coloured rule); the total's label stays quiet and only its number is bold. Then the **Balance credit** band (bordered) and the right-aligned "Updated HH:MM" footer. Rows 20–30 above keep their meaning; only the rendering changes.
- **62. Money signs.** The ₹ sign is on the headline figures only — available and Balance credit; the panel lines and the `used / pool` figure are unsigned (`formatwosign`).
- **63. Sheet behaviour** (user, "do not take every property of v1"): a backdrop that closes the sheet on press; dismissal by the pan-down **gesture**, the **backdrop** and the **Close** button; **dynamic sizing to the content** (`enableDynamicSizing`, no fixed snap points — the sheet was pinned at 70 %). The handle, the surface background and the Android back-handler hook stay as they are.
- **64. States.** UNLIMITED: no ring, no percentage and **no word** — where the ring would be there is nothing, and the bar's slot carries **v1's linear-gradient `ProgressSVG` at 100 %** (user, same day); the credit-line panel shows only Advance deposits, and there is no used/pool pair, no available figure and no balance band. BLOCKED is an ordinary capped line: full error ring, "100 %", literal zeros in the two credit-line rows. Only `credit.read` gates opening the sheet; the credit state never does.

**Device run, iOS simulator, 2026-09-06** (PR #51 build, iPhone 17e / iOS 26.5; passes and findings recorded in `docs/screens/customers-v2.md`). Four decisions follow from it; "start" given the same day.

- **65. The list returns to the top when the list changes.** A filter, sort, direction or search change replaces page 1, and the reader starts reading again — so the list scrolls to offset 0, unanimated, whenever the request key changes. FlashList 2 keeps the first visible row in place by default (`maintainVisibleContentPosition`), which is right for a page append and wrong for a re-sort: after Reset from Name Wise the list opened on row 5. Page appends and pull-to-refresh keep their position.
- **66. The keyboard yields to the list.** With the search focused, the first tap on a row was swallowed dismissing the keyboard (React Native's `keyboardShouldPersistTaps="never"`), so the ring, the tiles and the chevron each needed two taps. Now: `keyboardShouldPersistTaps="handled"` and `keyboardDismissMode="on-drag"` on the list; `Keyboard.dismiss()` as the credit sheet opens and as any row target navigates; and a tap on free area — the header card, the chrome around the controls, the surface below the last row — dismisses it. A chip press applies its filter as before.
- **67. The screen follows F-APP-7 at every text size** (see `02-foundations.md`). Chips, the search pill, the status chip, the money tiles and the header card's captions and values take their heights from their type role through `useTypeScale`, never from a constant; the chips row **wraps** (the sort chip keeps its right edge on whatever line it lands, via `marginLeft: 'auto'`); at the accessibility breakpoint the two tiles **stack** full-width and the header card becomes three rows with the value colours as the legend; `layout.js`'s column equation holds at every `fontScale`; the sort menu's items are uncapped (`titleMaxFontSizeMultiplier`) and its width derives from the scale, clamped to the window. At 1× nothing moves.
- **68. The credit sheet follows F-APP-7.** Header: the Close slot is sized by its own text (64 pt is the minimum, not the width), the title may take two lines, the header's height derives from `titleMedium`. At the breakpoint: the summary card's `used / pool` and `available` take two lines, the two panels become **one column** (Credit line above Used, each full width), the balance band stacks its label over its number. Numbers are `numberOfLines={1}` and never shrink; labels ellipsise. The sheet keeps `enableDynamicSizing`, so it grows and then scrolls, as before.

**Two more foundations applied to this screen (2026-09-07, "plan them and add them"; F-APP-9 / F-APP-10 in `02-foundations.md`).**

- **69. Hindi.** Every string the screen shows — title, search placeholder, chip labels, the six sort labels (looked up by key at render), the header card's captions, the status chips, the reset Alert, the empty and error states, and the whole credit sheet — has Hindi copy in `strings.hi.js`, read through `useStrings`. Numerals and money stay as they are (`₹ 1,05,769.39`); `CR` `DR` `OTP` stay Latin. The header title is set by the screen through `navigation.setOptions` so it follows the language. Devanagari renders on the taller line (F-APP-10 §4) and the boxes follow it. A language change remounts the screen (mechanism 6).
- **70. Orientation and width.** The screen's body sits in a `Container`: full width in `compact` (< 600 pt), a centred 640 pt column from `medium` up. From `medium` the chips row and the header card share one line — chips on the left, card on the right — which is what a phone on its side (844 × 390) needs to keep rows on screen and what a tablet's width is for. The credit sheet is contained the same way, centred at 640 pt on a wide window. Nothing else moves: the row, the tiles' width rule (`tilesStack`) and the menu already read the window.

- **71. What a tap lands on (2026-09-08, on the simulator: "decrease the pressable area of the name row, enlarge the tiles' pressable top, and use the space above the name row"; amended the same day after "seems they will mistake the press").** The row's three targets tile its text column from the row's top edge to the bottom padding, with no overlap and no dead strip, and the one boundary a user cannot see sits at the VISUAL MIDPOINT between the two things they can: the name's letters and the tile's top edge, so a miss goes to the nearer visible thing. The name row's target starts at the ROW'S TOP EDGE — the 10 pt row padding above it is its hit slop — and runs 6 pt below its line box (`NAME_REACH`); the rest of the 9 pt strip under the line and the 6 pt column gap are the tile's reach upward (`TILE_REACH = 9`). At 1× that is 49 pt for Settings (10 + 9 + 24 + 6, where it was 42) and 47 pt for each money tile (9 + 32 + 6, where it was the bare 44); both clear the platform's 44 and a test guards that. The first cut put the boundary at the bottom of the name's line box (43 / 53) — 2.5 pt under the letters with the tile 15 pt further down, which sends a thumb that lands low on the name into the tile; the midpoint rule replaced it before the device run. Nothing moves on screen: the slot keeps `nameRowMinHeight` and the name stays centred in it at every text size; the strip is the slot's own bottom padding, so the handover is exact when a name wraps and at every scale (spacing does not scale). Side by side the two tiles meet in the middle of their 8 pt gap and stacked they meet in the middle of the gap between them (`tileHitSlop`); the tile's 6 pt reach down stays inside the row's 10 pt bottom padding, so no target crosses the divider into the next customer. RN 0.84's Fabric folds hit slop into the overflow inset on both platforms (`YogaLayoutableShadowNode.cpp`, `TouchTargetHelper.kt`), which is why a reach that crosses the tiles' wrapper is safe.
- **72. The filter chip names the state it is in (2026-09-08, "change status text to unverified when unverified filter is selected" → "what if we remove the glyphs and just use colors?").** The two tri-state chips carry a WORD per state, and nothing else changes with the state but the fill: any = outline, grey text, the filter's name ("Verified" · "Has Trans."); only = filled primary, white text, the same name; exclude = filled primary, white text, the state's word ("Unverified" · "No Trans."; Hindi "असत्यापित" · "बिना लेन-देन"). The ✓ / ✕ glyphs the chips inherited from the retired filter sheet are gone — once the word names the state a glyph repeats it, "✕ Unverified" is a double negative, and at 2.143× the glyph and its gap cost 32 pt of a row that already wraps. The error fill on exclude goes with them: excluding is not an error, and "Unverified" filled the same way as "Verified" is the same grammar ("showing these"). The outline "any" look is what tells the user a third tap clears the filter; the accessibility value still carries the state (any / only / exclude) and the accessibility label reads the word on the chip, so nothing is said by colour alone. Decision-67 test 3 (the glyphs grow with the text) is about a thing that no longer exists and is replaced by pins on the three looks and the state words in both languages. **Amended the same day, after the device run ("for unverified filter chip and No Trans. filter chip we want error color with opacity. selected state should have primary color with opacity … also fix width of filter chip so that it does not change on changing state"):** the two active states are WASHES, not solid fills, and they differ in hue — only = primary at the house tint (the 0.12 wash the sort button and the money tiles wear) with primary text; exclude = error at the same tint (the wash the row's Blacklisted badge wears) with error text; any = the outline on card with grey text, unchanged. No border on the two washed states, as on the tiles. The chip's WIDTH is fixed across its states: it carries both of its words, the visible one and the other as a ghost — zero height, zero opacity, hidden from assistive tech — stacked in a column under it, so the pill is as wide as the wider word in the current language at the current text size and never moves when the state flips. The outline "Verified" chip is therefore as wide as "Unverified"; a stable row was judged worth that.
- **73. The sort control on its own line when the chips wrap (2026-09-08, "in large font size, the chips go to next line. should that be the solution? how to make it look good in large font size?" → "decision 73 is ok").** Wrapping stays: at a large text size three chips do not fit a 390 pt row, a horizontal strip would hide which filters are applied, and shrinking text is forbidden. What looked wrong was the sort chip alone on the second line, right-aligned with a hole beside it — a chip that wrapped by accident. Now, whenever the row cannot hold the filter chips and the sort chip on one line, the sort control becomes a FULL-WIDTH ROW under the filter chips: the sort name at the left, the direction arrow at the right edge, same height, outline and radius as the chip. Two lines then read as two intentional rows — which customers above, in what order below. Nothing is hidden and nothing shrinks; at normal text size nothing changes. The decision is made from MEASUREMENTS, not from a width formula: the row, each filter chip and the sort label report their laid-out widths (`onLayout`), and `sortWraps = filterChipsWidth + gaps + sortChipNaturalWidth > rowInnerWidth`, so it holds in Hindi, at every text size, and flips back to a chip when the window widens (rotation, a tablet). Rejected: an arrow-only sort chip at large text (keeps one line, but a large-text user would no longer see which sort is applied without opening the menu — the wrong trade for an accessibility feature).
- **74. The panel on a large, tall window (2026-09-13, one reference image "for big screen" → "the design for large height and width screen like ipad"; "do not take texts from image it is just for reference").** In `expanded / tall` only (an iPad or a Fold open on its side — not `medium`, so an iPad or a Fold held upright keeps decision 70), the screen is two columns. A PANEL on the leading edge, `PANEL_WIDTH` in points (starts at 360, tuned on the device — "may vary check device and act accordingly" — with a floor of 320 dp, "as per real android devices in market"), holds, top to bottom: a header row drawn by the screen (drawer hamburger, title, the reset when something is applied) with the navigator's header hidden in this cell ("confirm navigator header hidden as we will also collapse it in small height screen"); the search pill and sort button; the two filter chips on ONE row as on the phone, with the sort control as decision 73's full-width row under them ("sort as built"; amended 2026-09-14, "keep verified and has trans. filter chips in same row in panel" — three stacked rows were the first build); the summary in its stacked layout (it no longer heads a table). The LIST PANE takes what is left, capped at `READABLE_WIDTH` 640 ("let it be in default at 640"); the user's refinement — on a foldable the pane takes one fold half so the split lands on the hinge ("would vary to take one of fold screen size if half folded device") — needs a hinge signal the app does not have ([[../06-all-screen-shapes-plan|06]] §4.3) and is recorded as the target for when it does. Rows in the pane are INLINE: avatar column, name with its caption line (status chip inline), the two money tiles side by side at the trailing side, the chevron at the edge; where `tilesStack(fontScale, paneWidth)` says the tiles no longer fit, the row falls back to the phone arrangement. The mockup's words ("Verified only", "Received today", "Balance due", "Sorted by") are reference only; decisions 69 and 72 stand. No navigation rail and no trailing side sheet for this screen — 05 §5's rail (C3) is not built. Spec: [[01c-customers-shapes]] §2.
- **75. The yield ladder (2026-09-13, "landscape means larger width smaller height can be done according to plan"; "yes fix defect by ladder"; "MIN_ROWS starts at 3").** Exactly [[../07-shape-questions-and-steps|07]] S3. Regions yield in rank: 1 the summary card (hidden), 2 the filter chips (collapsed to one button carrying the applied count, which opens the chips in a bottom sheet — the 05 §2 sanctioned answer; chips still apply on press), 3 the navigator header (`headerShown: false`); tabs do not apply (Customers is in the drawer stack). Search and the list never yield. A SHORT window (`compactHeight`) holds ranks 1–2 always; rank 3 and any further pressure come from the budget: `listBudget = (windowHeight − keyboardInset) − header − chrome` measured at the live `fontScale`, short while `listBudget < MIN_ROWS × rowHeight`, latched on when the keyboard opens and released when it closes — never on scroll. `MIN_ROWS = 3` (floor 2), pinned on a device with the ladder running (07 S8). This one rule fixes the landscape-phone cell and 05 §9.2 (zero rows at 2.143 in portrait with the keyboard open); there is no separate fix. Spec: [[01c-customers-shapes]] §3.
- **76. The keyboard inset goes into the list's padding; the root inset stays (2026-09-13, "keep bottom inset for now").** `listPadding.paddingBottom` becomes `insets.bottom + spacing.md + keyboardInset` (`useKeyboardInset()`, iOS net of the home indicator, Android as reported — 05 §10). The double-counted bottom inset (root `AppNavigatorContainer` + the list, 05 §1) is NOT changed now: it is v1's root inset and goes when v1 goes. And the card in the shared row of decision 70 is always stacked: the invariant "the card and the row ask `tilesStack` of the same width" holds only while the card heads the list (`compact`); in the shared row and in the panel the card heads nothing, so it stacks and the row keeps asking the pane's width (05 §1.3 resolved).
- **77. The collapsed Filters button joins the search row in a short window (proposed 2026-09-13, "in landscape mode can we adjust filter button in left of sort icon making search text width smaller from right?"; STARTED 2026-09-14, "start with filter button new position when height is less for short windows").** In a SHORT window (`compactHeight`, ladder rank ≥ 2) the collapsed "Filters n" button sits between the search pill and the sort button, and the pill gives up the width from the right; the collapsed band under the search row disappears, which returns ≈34 pt at 1× and ≈55 pt at 2.143 to the list in exactly the cell that is starved of height (a phone on its side at 1× has ≈134 pt of list — one row). Only in a short window: in a tall window with the keyboard open a 390 pt phone would leave the pill too narrow, so there the button keeps its own row. Measured, never assumed (decision 73's pattern): when the three do not fit the line at the current text size the button drops back to its own row. `chromeHeight` counts the collapsed band as 0 in the short cell; the shapes suite asserts the placement per preset. Built test-first 2026-09-14 on the same branch; seen on the 17e simulator on its side the same day (1×, AX-L, AX-L + keyboard).
- **78. The legend card heads the list in `medium / tall` too — decision 70's shared row retired (2026-09-13, "also we see compact summary card by default in ipad mini. why? we have enough space there right?" → "apply clean fix for summary card for medium and tall screens").** Why the iPad mini upright showed the stacked card: decision 70 put the chips and the card side by side from `medium` up, half the 640 column each, and decision 76 stacked the card in that row always (it headed nothing there, and 316 pt never fits three columns). The shared row was drawn for a phone on its side, which is now a SHORT window where the ladder hides the card outright — so it only ever appeared in `medium / tall` (iPad upright, Fold opened upright, Split View ½), which has height to spare. The fix: in every cell that is neither the panel (74) nor short, the chrome stacks exactly as on the phone INSIDE the contained 640 column — search row, wrapping chips (73), the card at full column width heading the list, the list. The card's layout is the width rule alone, `tilesStack(fontScale, contentWidth)`: the three-column legend at 640 and normal text, stacked at the sizes where the row's tiles stack; the card and the row ask the same width by construction, which restores the original invariant of `layout.js`. What decision 70 keeps: the contained body (`Container` 640) and the credit sheet centred at the measure. `chromeHeight`'s shared-row arithmetic goes with it. Built 2026-09-13 on the same branch.
- **79. The panel takes a short window that can hold it (2026-09-14, "can we use split screen for details and list for landscape orientation, like we do it for ipad screen. we would not have to ladder hide screens?" → "ok keep the minimum point of device width to be 320dp. i agree with changes for panel and ladder above. start").** A phone on its side is starved of height and rich in width; the ladder (75) spends height to hide chrome, the panel (74) spends width to keep it. On the 17e on its side at 1× the ladder leaves 278 pt of list (2.7 rows, summary hidden, chips collapsed) and the panel 378 pt (3.7 rows, every control visible); with a 200 pt keyboard 122 vs 178 pt; at AX-L one row vs one and a half. So the panel is the layout for any SHORT window whose usable width (the window less the left and right safe-area insets) is at least `PANEL_WIDTH_SHORT` 300 + `PANE_MIN_WIDTH` 320 = 620 — the 320 is the compact floor of 06 §4.1, kept as the least a list pane may be ("minimum point of device width 320dp"), and the panel is NARROWER in a short window than in a tall one (300 pt against the iPad's 360; "we could also change the ratio of panel width from 50% to be lesser" — in points, never a percentage, per 07 Q3 rule 10). A phone on its side (844 − 94 = 750 → pane 450), an iPhone SE on its side (667 → 367), a Fold's cover on its side (≈970 → 640) and a dragged-short iPad window that is wide enough all get the panel: its own header row, the column that scrolls, the pane `min(640, usable − 300)`, rows falling back to the phone arrangement where `tilesStack` says so, nothing hidden and no latch. The ladder keeps its two jobs the panel cannot do: portrait with the keyboard open (390 wide, the cell of 05 §9.2), and short windows narrower than 620 usable — the Flip cover (400 × 440), a small dragged window — where decision 77's Filters button in the search row also stays. And one thing decision 79 exposed the same evening: the inline row (74) fell back only where `tilesStack` said the tiles no longer fit, which is the wrong question beside a name — in a 390 pt pane the names vanished. The row now goes inline only where `inlineFits` leaves a name at least six ems of its face (`NAME_MIN_EM`); a phone on its side falls back to the phone arrangement with the names whole, the iPad stays inline **(superseded by 80: the inline row is retired everywhere, and `inlineFits` / `NAME_MIN_EM` with it — every pane row is the phone's row)**. Tall `medium` windows (an iPad or a Fold held upright, Split View ½) are untouched: the user's "expanded only" for tall windows stands and decision 78's legend card heads the list there. Built test-first 2026-09-14 on the same branch and seen on the 17e simulator on its side the same evening (1×, AX-L, AX-L + keyboard) before the commit.
- **80. The pane's row is the phone's row (2026-09-14, "in when we use panel in landscape mode, the name of customer in list can use whole row. make buttons of daily total and a/c balance align from bottom to let space for customer name. it is already like that right?" → the assessment answered: only on the narrow panes, not on the wide ones → "start").** In the panel's list pane — decision 74's `expanded / tall` and decision 79's short windows with at least 620 pt of usable width — EVERY row is now the phone's arrangement: the name slot across the whole text column (the status chip and the chevron on the name's line, exactly as on the phone), and the two money tiles UNDER it, side by side at their `TILE_FLEX` weights or stacked when `tilesStack(fontScale, paneWidth)` says so. The tiles end level with the tag line under the avatar because the name slot absorbs the slack (`columnsAlign`) — which is what "align from bottom to let space for customer name" is asking for. The ONE-LINE inline arrangement of decision 74 is RETIRED, and decision 79's `inlineFits` / `NAME_MIN_EM` / `nameMinWidth` go with it: they existed only to choose between the inline row and this one, and there is no longer a choice to make. `CustomerRow`'s prop `inline` is renamed `pane`, and its one remaining job is naming the width `tilesStack` is asked of — `paneWidth(usable width, panelWidthFor({ compactHeight }))` inside the pane, `contentWidth` everywhere else; decision 79's usable-width reasoning stands unchanged. Where it shows: the iPad's 640 pt pane, a Fold open on its side (≈540), a 17 Pro Max on its side (≈538 once its 59 pt gutters are paid — computed, not yet seen). The 17e (450) and an SE (367) on their side already drew this shape through 79's fallback, so nothing changes there. Before, in a 640 pt pane at 1× the name had ≈133 pt beside two amounts and a chevron; now it has the whole text column, ≈526 pt. The COST is none in height: the inline row and the phone row were the same height at every text size, because the avatar column sets the row and `columnsAlign` makes the text column meet it, and both grow only when `tilesStack` stacks the tiles. A name that wraps to two lines grows the phone row where the inline row absorbed it — but at ≈526 pt a second line is rare. **Asked and answered the same day** ("should we use safearea in left and right when landscape mode? or should we leave it due to notch?"): the side insets are KEPT. `AppNavigatorContainer` pads all four safe-area insets around every screen and paints the gutter in the page background, so nothing sits under the Dynamic Island / notch or under the rounded corners; that is what iOS's own landscape layouts do (backgrounds may bleed, content stays inside the safe area) and what Android's display-cutout insets ask for under edge-to-edge. Dropping them would also break decision 79's arithmetic, which is built on the USABLE width — the 17e's pane would read 544 instead of 450. The polish that is possible later, not in v1: let the list surface and its dividers run under the side gutters while the row's content stays inset, as iOS tables do. Built test-first 2026-09-14 on `app_screen_customers_design`, uncommitted, awaiting the user's review of the diff; the device look — an iPad or a 17 Pro Max simulator on its side — is still to do. Spec: [[01c-customers-shapes]] §2.
- **81. The daily tile rounds every line, then sums (2026-09-15, from screen 02: "round every line than sum it for summary, we will see no difference that way").** `today.amount` stops using v3's `getPtSum` / `getCsSum`, which round the relation-day SUM once (half-to-even), and adopts the Daily Summary money rule in `api_v4/lib/money.js`: each product line rounded to the paisa in integers (half-up), each order's cash reimbursement to the paisa, then summed. So the tile a dealer taps and the Daily Summary card it opens can never differ, and neither can a selection of every order on that card. `summary.dailyTotal` and `sort: "daily"` follow automatically. The two aggregates stay two, so the pinned round-trip count does not move; v3 is not edited. Built as its own red/green pair (Step G) on the Daily Summary API branch `api_screen_daily_summary`, not on PR #40, so it can be committed or dropped on its own. Rounding is half-up to the paisa, never a trim (user, 2026-09-16); a line worth exactly half a paisa can be one paisa above v3's floating-point rounding, accepted. Status: building, uncommitted. Spec: [[02-daily-summary]] O‑3.
- **82. A never-ordered customer is the earliest "latest order" — the direction arrow mirrors the list (2026-09-23, "when order wise sorting is selected, we get the latest order by customer in first place. but when we change the sorting from ascending to descending we do not get the first customer in the last. the last customer is one having no transaction at all. is the sorting correct?" → "go as suggested fix").** Decision 36 pinned customers with no sales order ever LAST in both directions, so that "oldest first" would not open with the never-bought. The cost showed in use: Order Wise ↓ then ↑ did not reverse the list — the never-ordered block stayed at the bottom and the customer that led ↓ sat just above it instead of closing ↑ — and an arrow that does not mirror the list reads as a bug. Now a missing date is the EARLIEST value: ↓ is unchanged (newest … oldest, then the never-ordered), ↑ opens with the never-ordered (`cust_id` ascending among themselves) and then oldest … newest, so ↑ is exactly ↓ reversed whenever the keys are distinct. Only `latestOrder` can be null, so the other five sorts do not move. One line in `compareRows` (`api_v4/readmodels/customers.js`); keyset paging follows because `pageSlice` shares the comparator and the null sentinel already round-trips through the cursor — now exercised on page 1 of an ascending walk by a new test. A dealer who opens ↑ to find who has gone longest without ordering sees the extreme case first; the Has Trans. ✕ chip narrows it (FY-scoped, so broader than "never"). No app change. Branch `api_v4_latest_order_mirror` off `api_v4_foundations` `2c4a305`; red then green in `test/api_v4/lib/customersModel.test.js` and `test/api_v4/screens/customers.test.js`. Status: built, uncommitted. Amends 36 and [[01b-customers-filter-sheet#5. API contract — what this sub-spec fixes in the parent|01b §5]]; §5.3 test list updated.

### 2c. Filter sheet — **sub-spec `01b-customers-filter-sheet.md`, session to be scheduled**

Frame: `../designs/customers/02-filter-sheet.png` (shared 2026-09-03). What it fixes for **this** spec's API contract (round 7): three tri-state status toggles (Verified · Has Trans. · Blacklisted), five sorts in the frame (Creation Date · A/c Balance · Daily Total · Credit Total · Latest Order wise) plus Name (added round 8, not yet drawn) with one direction arrow on the selected sort, "\* At least one required", "Reset to Default". Everything about its layout, states and copy is planned in `01b`; the API keys it needs are already in §5.1 so the sheet session cannot change the contract except O‑2.

Fields v3 returns that **no element uses** and v4 therefore does not return: `dealer_id.{dealer_name, oil_do, dealer_img}`, `cust_id.otp_mgr`, `cust_code` (search is by name only — decided 2026-09-03), `ftank_amt`, `cust_verified`, `monthly`, `unapproved` (badge dropped 2026-09-03, confirmed round 7), `createdAt` (sort happens on the server), `closed` (a model field nothing writes — round 6), `hasTrans` (a filter input, never displayed), raw `cs_reimb`/`dvr_otp`/`products` (folded into `tags`).

Rule: **if it is not in this table, the API does not return it.**

## 3. What the user can do ⛔

| #   | Action                                                                                                            | Trigger                               | Precondition (business rule)                                      | API command / call                                                              | Optimistic? | Success feedback                                          | Failure feedback               |
| --- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------- | ------------------------------ |
| 1   | Search by customer **name**, matching anywhere, case-insensitive                                                  | typing (debounced)                    | —                                                                 | read model body `q`                                                             | n/a         | list replaced from page 1, scrolled to top; totals follow | screen error                   |
| 2   | Status filters, each tri-state any / only / exclude: **Verified · Has Trans. · Blacklisted**                      | filter sheet → Done                   | —                                                                 | read model body `filters` **+** `PUT /api/v4/users/prefs/dealer_customers`      | n/a         | list refetches from page 1; badge and totals update       | screen error; pref save silent |
| 3   | Sort: **Creation Date** (default) · Name · A/c Balance · Daily Total · Credit Total · Latest Order wise, asc/desc | filter sheet → Done                   | —                                                                 | read model body `sort`, `dir` **+** `PUT /api/v4/users/prefs/dealer_customers`  | n/a         | list refetches from page 1                                | as above                       |
| 4   | Reset to Default                                                                                                  | filter sheet button                   | —                                                                 | read model with defaults **+** `PUT …/prefs/dealer_customers` with the defaults | n/a         | badge disappears; list refetches                          | as above                       |
| 5   | Load next page (infinite scroll, 12 rows per page)                                                                | end reached                           | `page.hasMore`                                                    | read model with `cursor`                                                        | n/a         | rows appended                                             | footer retry                   |
| 6   | Refresh                                                                                                           | pull-to-refresh · header icon · focus | —                                                                 | refetch page 1 (cache invalidated)                                              | n/a         | spinner                                                   | screen error                   |
| 7   | Open customer settings                                                                                            | avatar / Settings strip               | scope ∈ {DPrimary, DAdmin} (client-side; `CustSettings` stays v3) | navigate `CustSettings`                                                         | n/a         | —                                                         | —                              |
| 8   | Open daily summary                                                                                                | daily tile                            | —                                                                 | navigate `DailySummary`                                                         | n/a         | —                                                         | —                              |
| 9   | Open ledger                                                                                                       | balance tile                          | —                                                                 | navigate `Accounts`                                                             | n/a         | —                                                         | —                              |
| 10  | Open credit summary                                                                                               | donut                                 | `credit.read` in the user's `amend_prem.allow`                    | none — v2 sheet rendered from the row's `credit` object (§2b)                   | n/a         | sheet                                                     | —                              |

**One command** (`PUT /api/v4/users/prefs/dealer_customers`, §5.2). Every other precondition is a client-side visibility rule and becomes a Tier 1 helper with a test. Server-side rules: tenancy (dealer from token), role (`authorize("dealer")`), and the prefs write touching only the caller's own document for the addressed company.

## 4. Design references ◌

- **Frames** (both 1× exports, 414 × 896; re-export at 2× before the design session if text is hard to read):
  - `../designs/customers/01-default.png` — the customer-side **"Dealers"** frame (shared 2026-09-03): header with search + Add Dealer + filter badge; summary strip Dealer 4/4 · Daily total · Total Balance; rows with CR/DR/OTP tags, oil-company logo, Settings strip, name, green daily tile, blue balance tile, credit donut with %, flat bar for unlimited. **Customers is designed "the same way"** with the substitutions at the top of §2. The row in this frame is the Customers row (round 7).
  - `../designs/customers/02-filter-sheet.png` — the **Customers filter sheet** (shared 2026-09-03): Close / Filters / Done; "Select Customer by Status" with Verified (✓) and Has Trans. (✕) toggles; "Select sorting Type" radio list Creation Date (selected, direction arrow) · A/c Balance · Daily Total · Credit Total · Latest Order wise; "\* Atleast one required"; "Reset to Default". The rows visible behind the sheet (initials avatar, "Daily Total"/"Balance" tiles, a numbered purple badge) are an older iteration — the badge is **not** part of the row (round 7).
  - If a live Figma file exists, add its URL to `../designs/README.md`.
- Design-system tokens: `src/theme/` does not exist yet (foundation F-APP-3, Phase 2). Colours seen in the frames — green daily tile, blue balance tile, primary purple, error red — map to tokens in the design session, not here.
- States that need their own frame: default, empty (no filter / with filter), first-page loading skeleton, next-page footer spinner, error, offline, unverified row, blacklisted row (chip), UNLIMITED row (flat bar), blocked row (`max_cr_lmt = 0`), credit sheet (capped / unlimited / blocked, with the "Updated" footer), font-scale 200 %. Filter-sheet states belong to `01b`.
- **Discussion-session notes (dated):**
  - **2026-09-17 — optimisation check, plan only.** The user asked whether both v2 screens and their APIs are optimised, for common components, and for crisp JSDoc-style comments ("just plan now we will implement later"). Four read-only audits → [[08-v2-optimisation-plan]] (PROPOSED; nothing built). For this screen: **RT‑1 / RT‑2** (every mounted row re-renders on each search keystroke and each keyboard show; each row subscribes to the window and the keyboard itself), **RD‑1** (`index.js` 1,506 → ~450), the comment pass (a 279-line file header); API **F‑3 / F‑4 / F‑5 / F‑6** (the whole sales history scanned per request; the v3 uninvoiced-SO helper is unbounded and O(N²); two missing indexes) → API‑1, API‑2.
  - **2026-09-03 — Step 1 session, round 1 (user + Fable).** Decisions:
    1. **Sequencing:** foundations first — Phase 0 → 1 → 2 per [[../02-foundations]], then Customers as screen 01. Spec agreed now, build waits for the foundations gate.
    2. **Location:** `src/screens/v2/Customers/` behind the registry and `screen_v2_customers` (D10 as written); the v1 path named in the request is what gets **retired**, not edited. _Superseded by 20 (round 3): the folder carries the role._
    3. **Roles:** everyone can view balances; Settings only DPrimary/DAdmin; credit % and sheet keep the `credit.read` gate.
    4. **Row actions:** Settings, daily tile, balance tile, credit sheet. The unapproved-payments badge (disabled in v1) is dropped — `unapproved` leaves the contract.
    5. **Credit sheet:** new v2 component (not `RelationCreditBS`).
    6. Closed/hidden excluded by default with an `includeClosed` body key. _Superseded by 28 (round 6): `closed` is a dead field and `hidden` means blacklisted._
    7. **Paging × sort:** all sorts server-side; infinite scroll with **12 rows per page**; status filters server-side.
    8. **Search:** server-side, matches **anywhere** (case-insensitive) — built like the vehicle search in `Customer/NewOrder/BSheets/SelectVehicle.js` / `api_v3/services/veh_trns.js:343-355` (escaped regex over the search field).
    9. **Preferences:** sort + dir + filters saved **per user per company**; the read model applies them when the body omits those keys.
    10. **Daily tile window:** server computes the IST day.
    11. **Tags:** `CR` = `cs_reimb`, `DR` = a discount set on a Diesel product, `OTP` = `dvr_otp`.
    12. **Summary strip:** count = matching / all; Daily total and Total Balance over **all** relations. _Totals part superseded by 24 (round 5)._
    13. **No Add Customer** on the dealer side; the customer-side Dealers screen is a future screen that will reuse this one's components.
    14. **Branches:** `api_v4_foundations` (from `api_tdd` @ `ecdd3ae`) and `app_v4_foundations` (from `app_tdd` @ `8b6831df`) created 2026-09-03; per-screen branch layout decided later.
  - **2026-09-03 — round 2 (user's follow-up).** Amendments: 15. **Search by name only**, not code (`cust_code` leaves the contract; placeholder "Search Customer name"). 16. **`DR` tag** is driven by the product's **PRIMARY category = "Diesel"** (`prod_msts.categories`), not by a product named HSD — a dealer can have several Diesel products; any of them with a discount on the relation sets the tag. 17. Credit sheet fetches fresh data on every tap via a second read model. _Superseded by 22 (round 4)._ 18. **Preferences storage:** the user asked whether a new collection is better than `users.companies[]`. Fable recommended **a new collection `user_prefs`**, one document per user × company, unique index `{ user_id: 1, co_id: 1 }` — structure and reasoning in §5.2. _Confirmed in round 5 (A‑5)._ 19. **Largest live dealer has 130 relations** → O‑1 resolved: the per-request pass over all relations (constant round trips) is acceptable; no cached balance needed. A perf test pins the round-trip count and the PR records the staging p95 for that dealer.
  - **2026-09-03 — round 3 (user's follow-up).** 20. **Location (supersedes 2):** the screen belongs to the dealer app, so the folder carries the role like the rest of the app: **`src/screens/v2/Dealer/Customers/`**, registry key **`Dealer/Customers`**, toggle **`screen_v2_dealer_customers`**. D10, the template, the foundations and the playbook now read `src/screens/v2/<Role>/<Name>/`, registry key `<Role>/<Route>`, toggle `screen_v2_<role>_<slug>` — required anyway because `Orders`, `Invoices` and `Payments` are route names in both role trees, so a bare route name cannot key the registry. 21. **`user_prefs` structure** requested — written out in §5.2 (one doc per user × company; `screens.dealer_customers` key named after the toggle; enums guarded by the v4 route schema, not the model). The command path follows the key: `PUT /api/v4/users/prefs/dealer_customers`.
  - **2026-09-03 — round 4 (user's follow-up).** 22. **Credit sheet = row data (A‑6 decided; supersedes 17).** User: "if we are efficient with single endpoint we will go with it." No second read model, no request on open; the per-customer credit endpoint, its test file and its store query are removed from this spec. Size returns to **S**. 23. **Refresh button in the sheet — no** (user asked; Fable recommended against: a refresh needs either the endpoint just removed or a page-1 refetch of the list, which recomputes all relations, resets the infinite-scroll pages and may drop the tapped row off page 1). Instead the sheet shows **"Updated HH:MM"** from the list response's `meta.generatedAt` (A‑7). _Confirmed in round 5._
  - **2026-09-03 — round 5 (user's follow-up: "keep page constant to 12 for api as well. total counts all relations but the filtered relation updates the daily total and total balance sum. rest is all ok").** 24. **Summary totals follow the filter (supersedes the totals part of 12):** `summary.all` still counts every relation of the dealer; `summary.matching`, `summary.dailyTotal` and `summary.totalBalance` are computed over the relations that pass the current `q` + `filters` — across all pages, not only the rows loaded so far. 25. **Page size is a constant 12 on the API too (A‑4 changed):** the read model has no `limit` key; `PAGE_SIZE = 12` is the endpoint's constant and `meta.limit` echoes 12. D6's "default 25, max 100" stays the general rule for other v4 lists; this screen's endpoint is fixed by spec. 26. **"Rest is all ok" → A‑1** (count universe), **A‑2** (avatar = `cust_img` with initials fallback), **A‑5** (`user_prefs` collection as written in §5.2), **A‑7** ("Updated HH:MM" footer) — **confirmed**. A‑3 (hidden rows never shown) was also confirmed, but on a wrong premise — reopened as A‑8 in round 6.
  - **2026-09-03 — round 6 (user: "count as two numbers. one filtered count and one total all relation count. what are hidden rows?").** 27. **Count = two numbers** — the spec's row 3: `summary.matching` (after filter + search) and `summary.all` (every relation). Confirmed. 28. **What `hidden` is (found in the code, supersedes 6 and A‑3):** `dealer_custs.hidden` is the **Blacklist switch** in Customer Settings (`CustSettings.js:1816-1824` writes `{ hidden: !blackListed }`); the order service refuses that customer's orders with "Please contact Dealer" (`api_v3/services/order_msts.js:265`). It is not a "soft delete". `dealer_custs.closed` ("transaction ended") has **no writer anywhere** in the API, the app or the web — a dead field. Consequences: `closed` leaves the contract; blacklist becomes a filter.
  - **2026-09-03 — round 7 (user: "a-8 is good we show blacklisted customer if filter pass. also added image for filter" + answers on the filter frame).** 29. **A‑8 decided:** blacklisted customers are shown whenever they pass the filters, with the chip and dimming (row 14); they count in `all`, and in `matching` and the totals when they pass. A third status toggle **Blacklisted** joins Verified and Has Trans. in the sheet. 30. **Filter frame read** (`02-filter-sheet.png`): status toggles are **tri-state** (blank = any, ✓ = only, ✕ = exclude), three of them; sorts are **Creation Date (default) · A/c Balance · Daily Total · Credit Total · Latest Order wise**; one direction arrow on the selected sort; "Reset to Default". The `name` sort Fable had proposed is **dropped** (not in the frame). _Re-added by the user in round 8 (35)._ 31. **"Has Trans." = any sales order, invoice or payment in the current financial year.** Correction to Fable's "no extra query" claim, on the user's challenge ("this screen does not pull ledger screen data"): the list read model does read the **ledger buckets** — `credit.boip` (the balance tile) is the FY opening plus the FY `month_crdrs` buckets, fetched once for all customers in one grouped aggregate — so _invoices and payments this FY_ are already known per customer (a customer with any FY bucket has them). _Sales orders this FY_ are **not** already loaded (only today's and the unbilled ones are), so one extra grouped query over `so_msts` for the FY is needed: `{ cust_id, count, lastOnDt }` per customer. That single query also serves "Latest Order wise" if O‑2 lands on sales orders. Round trips stay constant (≈ 9). 32. **"Latest Order wise" definition deferred (O‑2):** user: "by default use creation date. then we have options to sort in image. we will plan the bottom sheet separately." The key `latestOrder` is reserved in the sort enum now; its date source (newest sales order `on_dt`, newest order request, or newest of any transaction) is fixed in the filter-sheet session and recorded here before "start API for customers". Its API test is `it.todo` until then. 33. **Row = the Dealers frame's row:** donut + %, **no numbered badge**; the rows behind the filter sheet are an older iteration. `unapproved` stays out of the contract. 34. **Filter sheet gets its own sub-spec** `01b-customers-filter-sheet.md` (per the playbook's L-screen rule applied to one sheet): layout, states, copy and the tri-state control. It cannot change §5.1 except O‑2.
  - **2026-09-03 — round 8 (user: "ok add name sort as well").** 35. **`name` sort added** (supersedes the drop in 30): key `name`, `cust_name` lower-cased, `asc` = A → Z. Six sorts in the contract; the filter sheet (`01b`) gains a "Customer Name" option — its position in the list and its label are `01b` decisions.
  - **2026-09-03 — "spec agreed"** (user, after round 8). O‑2 stays open by agreement and is owned by `01b`. Step 1 closed.
  - **2026-09-04 — `01b` filter-sheet session** (user, four answers, all on the recommended option). 36. **O‑2 closed:** `latestOrder` = newest `so_msts.on_dt` per customer, **all time**, from the same grouped query as `hasTrans` (which keeps its FY count as a conditional sum) — zero extra round trips; no-order customers last in both directions _(amended by 82 on 2026-09-23: last descending, first ascending)_. 37. The frame's "\* Atleast one required" is removed (nothing can be invalid). 38. Name sort is labelled **"Customer Name"** and sits second in the list. Sheet layout, states, copy, the tri-state control and four build assumptions (A‑1…A‑4) are in [[01b-customers-filter-sheet]]; a row-level "Last order …" line is a Step-3 proposal (P‑1), not a decision.
  - **2026-09-04 — API built (Step 2), PR #40.** Opus agent, seven commits (`22ffc59` red → `d2639ee` prefs; `da28798` red → `39ac24e`/`615c0fd`/`d979ada` read model; `53dbd18` fixtures), +177 tests (screen 113 · shaper 40 · prefs 22 · 2 captures), `test:full` 1328 green, **11 round trips constant** (10 when the body carries sort/dir/filters), pinned by a test. Three contract clarifications taken by the builder, ratified here: 39. **Body override is all-or-nothing** — any of `sort`/`dir`/`filters` in the body makes `prefsSource: "body"` and the other two fall back to the **defaults**, not the saved pref (one `prefsSource` can only name one source; the app always sends all three or none). 40. **Prefs replay** keeps `_id`/`createdAt` and content but each `screens.<key>.updatedAt` is refreshed (§5.2's diagnosability rule wins over §5.3's shorthand). 41. **`cursor: null` is a 400** — the client omits the key on page 1. Cosmetic: the prefs test file is `test/api_v4/commands/user_prefs.test.js` (matches the model name). Governance: `models/user_prefs.js` added; `api_v3/services/dealer_custs.js` gained **export lines only** for `getCsSum`, `getPtSum`, `pendingPOlist`, `uninvoicedSOlist`. **O‑3 (resolved 2026-09-04 by capture-time scenario setup — see the decision log; no seed change):** the seed has only two customers and four identical relations (all verified, none blacklisted, no discounts, `max_cr_lmt` null), so `fixtures/api_v4/screens_customers.json` has 2 rows — the app's Tier 3 decision tests (blacklisted row, DR tag, capped/blocked credit, page 2) need a **test-only seed enrichment PR** (`test/api_v3/temp/seed/v3/` factories) before or with "start screen customers"; hand-rolled success bodies stay forbidden.
  - **2026-09-04 — Screen built (Step 4), app PR #50 (stacked on #49), design session skipped by the user's gate.** Parallel Opus agents (user: "use agent teams"): Tier 1 `496b260b`/`6edbbf0f` + `b81322fc`/`056dc65c` (105 tests: `initials`, `canOpenSettings`, `canReadCredit`, `activeFilterCount`, `creditSummary`, `formatUpdatedAt`, `SORT_OPTIONS`/`defaultDirFor`/`cycleTriState`/`toRequestBody`); Tier 2 `cc0915e8` pull + `ca2d895c`/`f457970d` (23 tests; endpoints `v4_screen_customers` — cache keyed on the request body minus `cursor`, load-more merges de-duplicated by `cust_id`, `providesTags ['relations']` — and `v4_prefs_dealer_customers`); Tier 3 `20d9146d` → `15d384da`/`d16facfe`/`e7d2bb6f`/`b5754ddd` (16 decision tests; folder `src/screens/v2/Dealer/Customers/` with `components/{Tags,CustomerRow,SummaryStrip,CreditSheet,FilterSheet,tint}.js`; cutover = `register('Dealer/Customers', { v1, v2 })` + `resolveScreen` in `Dealer/Main.js`, pinned by importing the real navigator). API side: scenario captures (`_states` incl. a BLOCKED line, `_page1/_page2`, `_filtered`, `_saved`) resolve O‑3 without a seed change. **Harness facts for later screens:** `renderScreen` sits below the app's `ThemeProvider`, so a v2 screen test wraps itself in a local `Harness`; headers are hidden in the harness stack (assert `headerRight` through a `setOptions` spy); FlashList `onEndReached`/`RefreshControl` are driven through props (no layout pass under Jest); the bottom-sheet mock renders children unconditionally, so sheets mount their body from state and Close clears it; RNTL 13 `toHaveTextContent` is exact-match. Judgement calls: row donut `animate={false}`; tints via `hex2rgba(colors.success|link|primary, …)` (unreviewed in dark mode — design session skipped). Correction: `Balance/Components.js` is only imported by the v1 Customers screen (not shared) — deletable at Step 6.
  - **2026-09-06 — Design corrections on the simulator build of PR #51 (nine rounds on the canvas, page "Final", artboards "Final v2", "sort menu open", "reset confirmation"; "start" given the same day).** 51. **Sort replaces the filter button:** a sort glyph (MaterialIcons `sort`, new local SVG) on the round button; press opens a **react-native-paper `Menu`** anchored under it with the six sorts, the selected one tinted with a check; picking applies immediately (`applyPrefs` with the sort's natural direction, one PUT). 52. **Two filters only, as always-visible tri-state chips under the search** — Verified, Has Trans. (any = outlined · only = filled primary + ✓ · exclude = filled error + ✕); a tap cycles and applies immediately; **Blacklisted is no longer a filter** (blacklisted customers stay listed with their chip; the API keeps accepting `filters.blacklisted`, the app never sends it; a stray saved value still counts for the reset). 53. **Sort chip** at the right of the chips shows the applied sort + direction arrow; press toggles asc/desc and applies. 54. **The 01b filter sheet retires** (Done / Reset to Default gone; each change applies and saves its preference at once). 55. **Reset lives in the header's top-right slot with the refresh glyph, shown only while the sort or a filter is non-default**; press raises a native Alert ("Reset filters and sort?" · Cancel / Reset); only a confirmed Reset puts the chips to any and the sort to Creation date ↓, saves the defaults and refetches; with defaults applied the slot is empty — pull-to-refresh and focus refresh the current query (the old header refresh is gone). 56. **Unverified / blacklisted rows keep full opacity:** the avatar takes the `disabled` (unverified) or `error` (blacklisted) colour for fill and initials, the name text takes 0.6 opacity, and the single status chip sits at the end of the name line left of the chevron; the name wraps to two lines instead of colliding with it. 57. **Avatar 56** so the tag line's bottom meets the tiles' bottom (ring 66, tiles 132 / 140, header card columns follow); **full-width separator** between rows. 58. Header card: first caption "Count"; captions wear the screen fill (user's commit `c861bedb`). 59. **Sort menu as pictured (user, same day):** the six sorts read **Date Wise · Name Wise · Order Wise · A/C Balance · Daily Balance · Credit Balance** in that order (keys unchanged: createdAt, name, latestOrder, balance, daily, credit; Name Wise stays the one ascending default); the selected item carries a 4 pt primary bar on its left edge and a bold primary title — no tint, no check; hairlines between items; menu right-aligned under the sort button (Paper's own flip). The sort chip uses the same labels. 60. Total balance in the header card at titleMedium so the card fits every width (user's commit `380e6c6d`). Rows 2, 6–9, 13–14 and §2c amended accordingly; `01b` is superseded by 52–55.
  - **2026-09-04 — Design session (Step 3, run after Step 4 on the simulator build), three rounds on a design canvas — https://claude.ai/code/artifact/f42a2a5e-7afb-4f63-9c4d-d401d565fe3e (page "Final"; earlier rounds on page 2). User's opening notes: the two gaps (top under the header, bottom band), the ring number, "redesign the row, keep every click".** Decisions, all "all ok" on round 3 + the caption card: 42. **Insets:** the top gap is `Screen` adding `insets.top` under the navigator's header and the bottom band is `insets.bottom` on the container — `Screen` gains an `edges` prop (F-APP-3 amendment, tested); Customers passes none and puts `insets.bottom` in the list's `contentContainerStyle` so rows scroll under the home indicator. 43. **Summary = header card with captions on its border** (labels "Customers" · "Today" · "Total balance" sit on the top border like a fieldset legend; values inside; count primary, today success, total balance `link` at titleLarge); its Today / Total-balance columns use the row's tile widths (136 / 145) so they sit over the tiles — **rows carry no labels** (rows 3–5, 11, 12 amended). 44. **Credit ring wraps the avatar** (44 circle, ring 3, gap 2): colour only — error > 0.85, primary under, **dotted hairline when UNLIMITED (supersedes the flat bar of row 13)**, full error ring when BLOCKED; **no % on the row** (the sheet has it); the ring-avatar is the credit-sheet target for `credit.read` — **amended 2026-09-04 (follow-up on PR #51): in every state, UNLIMITED included, as v1 did** — otherwise it shows and nothing opens. 45. **Tags** `CR · DR · OTP` as plain primary text under the avatar, bullet-separated, no chip background (row 6 amended); **follow-up 2026-09-04: 9 pt caption face (`captionTiny` token in `src/theme/tokens/typography.js`, deliberately not a role) — "just to show".** **Blacklisted** (error tint) and **Unverified** (grey tint, new — explains the dimming) are small chips under the name; dimming 0.6 stays (rows 8, 14). 46. **Settings = a right chevron ending the name line**; the name row is the target, DPrimary / DAdmin only, no chevron otherwise; the Settings strip and the gear are gone (row 9 amended). 47. **Tiles:** amount only, 32 pt + hit slop (44 pt target), tinted `success` / `link` 0.12, radius md; today → DailySummary, balance → Accounts unchanged. 48. **Search pill 44 + round filter button 44 with the badge; applied-filter chips under the search** (new: each active tri-state and a non-default sort as a chip; tapping any chip opens the filter sheet; the badge count stays). 49. **List surface:** white sheet (radius lg top), inset hairlines, row ≈ 100 pt, no row-level tap. 50. **Copy accepted:** "Customers", "Today", "Total balance", "Unverified". Ring sizing follows the OS font scale (`useWindowDimensions().fontScale`), never a cap. Round history: A "Touch-ups" (built layout fixed) + B "Statement" (ring-avatar, ledger column) → round 2 "Ledger" (header as legend, gear badge) → round 3 (chevron, B's ring, text tags) → caption card. Build: one restyle PR stacked on #50, test-first; existing Tier 3 decision tests keep their test ids; new tests for `Screen` edges, the ring-avatar states, the chip row, and the header/tile column widths.

## 5. API contract (v4) ⛔

### 5.1 Read model — the list

```
POST /api/v4/screens/customers                       (body carries filters → POST, per D2)
Auth: Bearer (protect) · Roles: authorize("dealer") · Tenant: dealer_id = tenantOf(req).dealer_id — never from the body
Page size: PAGE_SIZE = 12, an endpoint constant (round 5) — there is no `limit` key; sending one is an unknown key → 400
Body (house DSL, unknown keys rejected at every level):
  {
    q?:       string, 1–60 chars, trimmed                                   // matches anywhere, case-insensitive, on cust_msts.cust_name only
    filters?: {                                                             // omitted ⇒ saved pref ⇒ {} (no filter); each key tri-state:
      verified?:    boolean,                                                //   absent = any · true = only verified · false = only unverified
      hasTrans?:    boolean,                                                //   absent = any · true = has FY transactions · false = has none   (round 7)
      blacklisted?: boolean                                                 //   absent = any · true = only blacklisted · false = exclude       (A‑8)
    },
    sort?:    "createdAt" | "name" | "balance" | "daily" | "credit" | "latestOrder"  // omitted ⇒ saved pref ⇒ "createdAt"; latestOrder = newest sales order, all time (O‑2, 01b)
    dir?:     "asc" | "desc",                                               // omitted ⇒ saved pref ⇒ "desc"
    cursor?:  string                                                        // opaque, from page.next
  }
200:
  {
    success: true,
    data: {
      summary: {
        all:          number,   // every relation of the dealer (blacklisted included) — never changes with q/filters
        matching:     number,   // relations passing q + filters
        dailyTotal:   number,   // Σ today.amount over the matching relations, all pages   (round 5)
        totalBalance: number    // Σ credit.boip  over the matching relations, all pages   (round 5)
      },
      customers: [
        {
          id:          { dealer_id: string, cust_id: string },                           // exit-point params
          cust:        { id: string, name: string, img: string|null },
          verified:    boolean,                                                          // dealer_custs.dealer_verified
          blacklisted: boolean,                                                          // dealer_custs.hidden (A‑8)
          tags:        { cr: boolean, dr: boolean, otp: boolean },
          today:       { amount: number },                                               // IST day, 2 dp
          credit:      { max_cr_lmt: number|null, adv_dep: number, boip: number, pendingPOs: number, uninvoicedSOs: number }   // also feeds the credit sheet (§2b)
        }
      ]
    },
    page: { next: string|null, hasMore: boolean },
    meta: { generatedAt: ISO, limit: 12, applied: { sort, dir, filters }, prefsSource: "body" | "saved" | "default" }   // generatedAt is shown in the credit sheet
  }
Errors: 400 VALIDATION (details[] with every path) · 401 UNAUTHENTICATED · 403 FORBIDDEN_ROLE (customer or superadmin bearer)
Partial failure policy: nothing optional — summary, today and credit are the screen's purpose; any sub-query failure fails the request.
```

**Filter definitions** (all evaluated on the server, per relation):

| `filters` key | true means                                                                                                                                                                                  | false means            |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| `verified`    | `dealer_custs.dealer_verified === true`                                                                                                                                                     | `=== false`            |
| `hasTrans`    | the relation has **at least one** of: a `month_crdrs` bucket in the current FY with `drttl` or `crttl` ≠ 0 (an invoice or a payment this FY), or a `so_msts` with `on_dt` in the current FY | none of those          |
| `blacklisted` | `dealer_custs.hidden === true`                                                                                                                                                              | `=== false` (or unset) |

**Sort keys** (ties broken by `cust_id` ascending; cursor = base64url `{ k, id }`, `PAGE_SIZE + 1` probe, no `countDocuments`):

| `sort`        | key                                                                                                                                                                                                                                                                         |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `createdAt`   | `dealer_custs.createdAt` — **default**, `desc`                                                                                                                                                                                                                              |
| `name`        | `cust_name` lower-cased (added round 8; `asc` = A → Z)                                                                                                                                                                                                                      |
| `balance`     | `credit.boip`                                                                                                                                                                                                                                                               |
| `daily`       | `today.amount`                                                                                                                                                                                                                                                              |
| `credit`      | utilisation ratio: UNLIMITED → 0; `max_cr_lmt + adv_dep` ≤ 0 → 1; else `(boip + pendingPOs + uninvoicedSOs) / (max_cr_lmt + adv_dep)` — the pool rule the v3 order check already applies; the read model reuses that exported helper (found in Step 2), never a second copy |
| `latestOrder` | **O‑2 closed 2026-09-04 ([[01b-customers-filter-sheet|01b]]):** newest `so_msts.on_dt` per customer over **all time** (the dealer's sale record — not the order request, not invoices/payments); comes from the same grouped `so_msts` query as `hasTrans`, zero extra round trips. Customers with no sales order ever sort **last in both directions**; ties by `cust_id` asc |

Because `balance`, `daily`, `credit` and `latestOrder` are computed, and the totals need every matching relation anyway, the read model does **one pass** per request: load the dealer's relations (≤ 130 today) → `summary.all` → compute `today`, `credit`, `hasTrans` and `lastOrderAt` for all of them in constant round trips → filter (`filters`, `q`) → `summary.matching`, `dailyTotal`, `totalBalance` over the filtered set → sort → keyset slice of 12. Rows can shift between pages when a payment lands mid-scroll; accepted for this screen.

**Composition rule** (from [[../00-overview#Risks|00 §Risks]]): the read model must **not** call `dlrCusts` or `allRelationCurrBal` (3 awaits × N relations). Constant round trips regardless of N, all `$in`/grouped by `cust_id`, run through `runParallel` after `dealer_custs.find`:

1. one `month_crdrs` aggregate for the FY grouped by `cust_id` → balance (must equal `calcFYCumulativeBal` per relation — the oracle test) **and** the invoice/payment half of `hasTrans`
2. `pendingPOlist({ dealer_id })` (already grouped)
3. `uninvoicedSOlist({ dealer_id })` (already grouped)
4. the two `so_msts` IST-day aggregates (`getCsSum`, `getPtSum`) → `today.amount`
5. **one `so_msts` aggregate over all time** (filtered by `dealer_id` + `cust_id ∈ relations`), grouped by `cust_id` → `{ lastOnDt: max(on_dt), fyCount: sum(on_dt in current FY) }` → `hasTrans` reads `fyCount > 0`, `latestOrder` reads `lastOnDt` (O‑2, 01b — same single query, no FY bound on the max)
6. `prod_msts.find({ dealer_id, categories: { $elemMatch: { group_name: "PRIMARY", name: "Diesel" } } })` projected to `_id` → `tags.dr`
7. `cust_msts.find({ _id: { $in } })` projected to `cust_name cust_img`
8. the caller's `user_prefs` doc, only when the body omits `sort`/`dir`/`filters`

`yearBalance` (opening) is pure and runs per row. Any change needed inside `ledger_window.js` is a separate test-first PR (D13).

### 5.2 Commands

```
PUT /api/v4/users/prefs/dealer_customers        (route param is an enum of screen keys — one entry per shipped v2 screen, each with its own body schema)
Auth: Bearer · authorize("dealer") · targets the caller (req.loggedInUser._id) and tenantOf(req).co_id — no ids in the body
Body: { sort: <sort enum>, dir: "asc"|"desc", filters: { verified?: boolean, hasTrans?: boolean, blacklisted?: boolean } }   (sort, dir, filters required; filters may be {}; unknown keys → 400)
200: { success: true, data: { prefs: { sort, dir, filters } } }
Idempotency: full replace by nature (PUT) — no Idempotency-Key
Write: UserPrefs.findOneAndUpdate({ user_id, co_id }, { $set: { "screens.dealer_customers": { ...body, updatedAt: now } } }, { upsert: true, returnDocument: "after" })
Side effect on Mongo (assert in test): exactly one user_prefs doc for { user_id, co_id }; screens.dealer_customers equals the body; a second company's doc untouched; other keys under screens untouched
Invalidation: none. The app sends explicit sort/dir/filters after a change; the list read model reads prefs only when the body omits them (one indexed findOne inside runParallel).
"Reset to Default" = this PUT with { sort: "createdAt", dir: "desc", filters: {} }.
```

**Storage — `models/user_prefs.js` (confirmed round 5; additive file, listed for approval in the brief). One document per user × company:**

```js
const UserPrefsSchema = new Schema(
  {
    user_id: { type: ObjectId, ref: "users", required: true }, // from the bearer, never from the body
    co_id: { type: ObjectId, required: true }, // from tenantOf(req) (the x-co-id company), never from the body
    screens: { type: Object, default: {} }, // Mixed on purpose — see rules below
  },
  { timestamps: true, minimize: false, strict: true },
)
UserPrefsSchema.index({ user_id: 1, co_id: 1 }, { unique: true })
```

As stored, after one save from this screen:

```json
{
  "_id": "…",
  "user_id": "6a4e…",
  "co_id": "6a4ec1673fafcaa9084668ce",
  "screens": {
    "dealer_customers": {
      "sort": "balance",
      "dir": "asc",
      "filters": { "verified": true, "hasTrans": false },
      "updatedAt": "2026-09-03T12:50:00.000Z"
    }
  },
  "createdAt": "…",
  "updatedAt": "…"
}
```

Rules:

- **Key naming:** `screens.<role>_<slug>`, the toggle name without its `screen_v2_` prefix (`dealer_customers` ↔ `screen_v2_dealer_customers`). A dealer company's doc holds only dealer-screen keys; a customer company's doc holds keys such as `customer_dealers`. No clash, no role field needed — the company decides the role.
- **`screens` is `Mixed` on purpose.** The route's v4 schema (D7 house validator, unit-tested) is the single guard for each key's enums. A new screen adds a route-param entry and a schema file; the model never changes again. `minimize: false` keeps `screens: {}` on a fresh doc.
- **Read path:** `UserPrefs.findOne({ user_id, co_id }, { "screens.dealer_customers": 1 }).lean()` inside the list read model's `runParallel`; a missing doc or key ⇒ defaults and `prefsSource: "default"`. Each key carries its own `updatedAt` so a stale pref can be diagnosed without the doc's timestamps.
- **Isolation:** nothing else reads the collection — auth, `check_user_company_status`, the 30-second user cache and the login/updaterx fixtures are untouched. Orphan docs after a user or company is removed are harmless and out of scope.

Why a separate collection rather than `users.companies[].prefs` (Fable, 2026-09-03): the users document is the auth document — `api_v3/auth.js` loads it on every request and caches it for 30 s, `check_user_company_status` reads `companies[]` on every request, and every login/`updaterx` fixture pins its shape. Preferences are high-write, low-value data; putting them there means every preference save invalidates the auth cache and grows the object attached to `req.loggedInUser`, and the schema change is a gated edit to the most sensitive model in the repo. A `user_prefs` doc per user × company is additive, indexed for a single upsert, invisible to auth, and future screens add a key under `screens` instead of another model change.

### 5.3 Test list — API (write these first, watch them fail)

`test/api_v4/screens/customers.test.js`

- [ ] happy path: dealer bearer, empty body → 200; `summary`, every row key in §5.1 present; numbers are numbers (2 dp); `max_cr_lmt: null` preserved (not coerced to 0); `meta.applied` = `{ sort: "createdAt", dir: "desc", filters: {} }`, `prefsSource: "default"`, `meta.limit === 12`; `meta.generatedAt` is a valid ISO instant
- [ ] tenancy: rows are only the token dealer's relations; `dealer_id` in the body → 400 VALIDATION (unknown key); customer bearer → 403 FORBIDDEN_ROLE; superadmin bearer → 403
- [ ] validation: `filters.verified: "yes"` → 400 with path `filters.verified`; `filters.closed: true` → 400 (unknown key); `limit: 100` in the body → 400 (unknown key — page size is a constant); `sort: "code"` → 400; `q` of 61 chars → 400; every failure listed in `details[]`
- [ ] filter `verified`: `true` returns only `verified: true`, `false` only `verified: false`, absent both; `summary.matching`, `dailyTotal` and `totalBalance` follow; `summary.all` does not change
- [ ] filter `hasTrans`: a relation with only a FY invoice → true; only a FY payment → true; only a FY sales order (no invoice yet) → true; a relation whose only activity is in the previous FY → false; a brand-new relation → false
- [ ] filter `blacklisted` (A‑8): a relation with `hidden: true` is returned when the filter is absent, with `blacklisted: true`; `true` returns only such rows; `false` excludes them; it counts in `summary.all` always and in `matching`/totals when it passes
- [ ] filters combine with AND: `{ verified: true, blacklisted: false }` returns verified, non-blacklisted rows only
- [ ] search: `q: "ust"` matches "Customer One" anywhere, case-insensitive; `q: "2"` does **not** match a customer whose `cust_code` is 2 (name only); regex metacharacters in `q` are literal (`"a.b"` matches nothing); `dailyTotal`/`totalBalance` cover only the matches
- [ ] sort: `createdAt`, `name`, `balance`, `daily`, `credit` — both directions, pinned against the seeded values; `name` is case-insensitive ("alpha" sorts before "Beta"); ties resolve by `cust_id`
- [ ] sort `latestOrder` (O‑2 closed 2026-09-04, tests in [[01b-customers-filter-sheet#5. API contract — what this sub-spec fixes in the parent|01b §5]]): `desc` = newest sales order first, `asc` = oldest first, pinned against seeded `on_dt`; a relation with no sales order ever sorts last descending and **first ascending** — `asc` is `desc` reversed (decision 82; was: last in both directions); an ascending walk whose page 1 is thirteen never-ordered relations pages on through the null cursor key; a relation whose only SO is in the previous FY sorts by it **and** reads `hasTrans: false` (all-time max, FY-bounded count from one aggregate); equal newest `on_dt` → `cust_id` asc; keyset paging stable after a new SO is inserted for a page-1 customer — **no `it.todo` remains**
- [ ] pagination: 13 seeded relations → page 1 has exactly 12 rows and `hasMore: true`; page 2 has 1 row, disjoint from page 1, `next: null`; `summary` identical on both pages; malformed cursor → 400
- [ ] `today.amount`: a SO at 23:30 IST yesterday is excluded, one at 00:30 IST today is included (server IST window; no dates accepted in the body)
- [ ] `credit.boip` equals v3 `getCurrBalance().boip` for the same relation (v3 ledger rule is the oracle — no second implementation); `pendingPOs` / `uninvoicedSOs` likewise
- [ ] `summary.dailyTotal` = Σ `today.amount` and `summary.totalBalance` = Σ `credit.boip` over the **matching** relations across all pages (assert against page 1 + page 2 rows), and both equal the all-relations sums when no filter is set
- [ ] tags: `cr` mirrors `cs_reimb`; `otp` mirrors `dvr_otp`; `dr` true only with a `products[]` discount (`disc_type !== "none"`, `disc_val > 0`) on a product whose PRIMARY category is Diesel; a discount on a Petrol product does not set `dr`; a Diesel product with `disc_type: "none"` does not set `dr`
- [ ] prefs applied: after `PUT /users/prefs/dealer_customers { sort: "balance", dir: "asc", filters: { verified: true } }`, an empty-body read returns rows in that order, verified only, `meta.applied` echoing it and `prefsSource: "saved"`; a body key overrides (`prefsSource: "body"`)
- [ ] no fan-out: query count for a dealer with 30 seeded relations equals the count for 3 (spy on the driver; constant round trips); PR records the staging p95 for the 130-relation dealer
- [ ] contract fixture captured: `yarn fixtures:export --set v4` → `fixtures/api_v4/screens_customers.json` (a seed with ≥ 13 relations so `hasMore` is exercised, at least one blacklisted, one without FY transactions)
- [ ] mutation smoke recorded in PR: remove `scopeFilter` → tenancy tests red; remove the IST window → day test red; drop the Diesel category match → `dr` tests red; sum totals before filtering → the two totals tests red; drop the SO half of `hasTrans` → the "only a FY sales order" case red

`test/api_v4/commands/users_prefs.test.js`

- [ ] green: 200, Mongo side effect — a `user_prefs` doc for `{ user_id, co_id }` with `screens.dealer_customers` equal to the body; a second company's doc untouched; a second PUT replaces (one doc, not two); `filters: {}` is stored as `{}` (not dropped)
- [ ] red: customer bearer → 403; missing `dir` → 400; `sort: "boip"` → 400; `filters.hasTrans: "yes"` → 400; unknown key → 400
- [ ] replay: same PUT twice → identical doc (idempotent)
- [ ] read-through: covered by the "prefs applied" test above

## 6. Screen build plan ⛔

- **Folder:** `src/screens/v2/Dealer/Customers/` (round 3: the role segment mirrors `src/screens/Dealer/`) — `index.js`, `useScreenModel.js`, `strings.js`, `components/{CustomerRow,SummaryStrip,FilterSheet,CreditSheet,Tags}.js`, `__tests__/Customers.test.js`. `FilterSheet` is built to the `01b` sub-spec. New shared UI that other screens will reuse (the customer-side Dealers screen first): `src/components/v2/{MoneyTile,CreditDonut,SearchField,TriStateToggle}` — final list in the design session.
- **Pure logic to extract first (Tier 1, TDD):**
  - `initials(name)` → `src/utils/Text/initials.js`
  - `canOpenSettings(scope)`, `canReadCredit(user, co_id)` → `src/helpers/Permissions/`
  - `activeFilterCount({ sort, dir, filters })` → `src/helpers/Filters/` (a `Filters` folder already exists) — counts a non-default sort/dir as one and each present `filters` key as one
  - `creditSummary(credit)` → `src/helpers/Credit/` — pool, available, maxOut, the UNLIMITED/BLOCKED display flags for the sheet (extends the existing tested module)
  - `formatUpdatedAt(iso)` → `src/utils/Dates/` — IST "HH:MM" for the sheet footer
  - `creditUtilization` — exists in `src/helpers/Credit/`, reused, not re-tested; `formatCurrency`/`formatwosign`/`formatCreditPct` — exist in `src/utils/Currency`
- **RTK Query endpoints (Tier 2, MSW):** `src/store/apis/v4/customers.js` —
  - `getScreen_Customers` as a **query** with infinite-scroll merge (`serializeQueryArgs` ignoring `cursor`, `merge` appends, `forceRefetch` on cursor change — the pattern already in `src/store/apis/paginationHelpers.js`), `providesTags: ['relations']` so the v3 `update_dealer_custs` mutation fired from `CustSettings` (including the Blacklist switch) still refetches the list (D9 shared tags); the latest page's `summary` and `meta` replace the cached ones on merge
  - `putPrefs_Customers` mutation, no tags
  - Tests: URL `<API_URL>/api/v4/screens/customers`, POST body carries exactly the keys set (no `undefined` placeholders, **no `limit`**, `filters` only with the keys the user set), bearer + `x-co-id`, page 2 appended and de-duplicated, a new `q` resets to page 1, 403 → `errorRTK` path; prefs URL `…/users/prefs/dealer_customers` as PUT
- **Screen test (Tier 3, RNTL, decision cases only)** — uses the pulled `v4_screens_customers.json`:
  - one row per fixture customer; name, daily amount, balance formatted
  - summary strip shows `matching / all`, daily total, total balance from the fixture; after a filter change the strip shows the new fixture's totals
  - Settings strip shown for `DPrimary`, hidden for `DView`
  - credit % label shown with `credit.read`, hidden without; donut in both cases
  - tags render per fixture booleans; unverified row dimmed; blacklisted row shows the chip
  - UNLIMITED row shows the flat bar, not a donut
  - loading skeleton / empty (two strings) / error states
  - tapping the tiles and the avatar navigates with the exact v1 params (§1 exit points); avatar tap does nothing for `DView`
  - filter sheet Done → refetch with the chosen `filters`/`sort`/`dir` **and** one `PUT /users/prefs/dealer_customers`; Reset to Default → refetch with defaults **and** one PUT with defaults; badge count matches `activeFilterCount`
  - end reached with `hasMore: true` → second request carries `cursor`; rows appended
  - typing in search → one request after the debounce with `q`, list back at page 1
  - donut tap with `credit.read` → the sheet shows the row's five numbers, the derived totals and "Updated HH:MM" from `meta.generatedAt`, and **no request was made** (MSW `onUnhandledRequest: "error"` proves it); donut tap without `credit.read` → nothing opens
- **Cutover:** route name `Customers` unchanged; registered in `src/navigation/screenRegistry.js` under the key `Dealer/Customers` behind `screen_v2_dealer_customers` (default on); `src/navigation/Dealer/Main.js:141` switches to `resolveScreen`; old component and `RelationFilterBS.js` deleted the release after.

- **Design restyle (2026-09-04, after the design session — one app PR stacked on #50, test-first):** `Screen` gains `edges` (F-APP-3 amendment, tested); new `components/CreditAvatar.js` (ring-avatar states, sized by `useWindowDimensions().fontScale`); `components/SummaryStrip.js` keeps its name and prop but becomes the header card (captions on the border, columns from `layout.js`); `components/AppliedChips.js` + `appliedChips()` in `src/helpers/Filters/customers.js`; `layout.js` holds the shared column constants; `Tags.js` becomes text with bullets; `CustomerRow.js` and `index.js` restyled. Existing decision tests keep their test ids where the element survives (`row-avatar`, `row-settings`, `row-daily`, `row-balance`, `row-tag-*`, `row-blacklisted`, `summary-*`, `filter-*`); `row-credit-pct` is retired (no % on the row) and `row-donut` / `row-flatbar` become `row-ring` with an accessibility value naming the state.

## 7. Definition of done

- [x] Spec agreed and dated (2026-09-03); both PNGs in `designs/customers/`
- [x] O‑2 closed in `01b` and copied into §5.1 — 2026-09-04
- [x] Credit sheet Option F on PR #51 (2026-09-06): red → green, case 20 pins dynamic sizing + the backdrop + the three dismissals, cases 6b/13 rewritten for the sign rule and the panel tones, docs + device checks updated
- [x] Final v2 on PR #51 (2026-09-06): two red → green pairs (row `945de2ae`→`5601cd1e`, chrome `282756e8`→`bfa2e036`), sort menu + chips + reset, filter sheet retired, 837 tests, docs updated — awaiting the user's merge
- [x] Design restyle PR #51 (2026-09-04): five red → green pairs (Screen edges, appliedChips + layout, CreditAvatar, row + header card, chrome), mutation smokes recorded in the PR, Tier 3 suite 17 → 18 + 3 chrome cases, docs updated, CI green, stacked on #50; **follow-ups 2026-09-04 (`00635735` → `8333ad91`): 9 pt `captionTiny` tags, unlimited ring opens the sheet — 836 tests, CI green** — awaiting the user's merge
- [ ] API PR: tests first (red commit → green commit), fixture exported, flow-map rows added in `docs/testing.md`, `yarn test:full` green; `models/user_prefs.js` approved in the brief; staging p95 for the 130-relation dealer recorded
- [ ] App PR: Tier 1 → Tier 2 → Tier 3 tests, `yarn fixtures:pull`, `yarn test` green, no-network guard intact
- [ ] Manual: one request on screen open (v4), none on credit-sheet open, old screen still works on v3 for a v1.78 build
- [ ] Release gate green; screen listed in the release notes; `screens/README.md` row updated
