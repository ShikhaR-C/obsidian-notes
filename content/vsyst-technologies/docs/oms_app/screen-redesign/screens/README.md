# Screens index

One row per screen, updated at every gate (see [[../03-per-screen-playbook#Tracking]]). The spec's status line and this table are the only two places status lives.

| Screen                               | Size | Spec agreed | API PR | Design agreed | App PR | Shipped in | Old screen removed | Notes                                                                                                                                                                  |
| ------------------------------------ | ---- | ----------- | ------ | ------------- | ------ | ---------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [[01-customers\|Customers]] (dealer) | S    | 2026-09-03  | #40 (ready 2026-09-04, stacked on #39; + scenario captures) | 2026-09-04 (decisions 42–50) + **2026-09-06 corrections (decisions 51–58, nine rounds; `01b` superseded)** | #50 (ready 2026-09-04, stacked on #49; 771 tests, gate PASS) + **#51 design restyle** (ready 2026-09-04, stacked on #50; **Final v2 corrections 2026-09-06 — sort menu, tri-state chips, reset, credit sheet Option F; 838 tests**) | —          | —                  | Step 1 closed 2026-09-03 (eight rounds, 35 decisions in spec §4); one read model (page constant 12, tri-state filters Verified/Has Trans./Blacklisted, six sorts incl. Name, totals follow the filter) + prefs command; credit sheet from row data; folder `src/screens/v2/Dealer/Customers/`; O‑2 closed 2026-09-04 in `01b` (newest sales order, all time); both frames in `designs/customers/`; Phases 0–2 built 2026-09-04 (API PR #39, web PR #27, app PR #49 — all awaiting the user's merge); next = user merges #39→#27→#49→#40→#50, then #51 (design restyle) + device checklist in `docs/screens/customers-v2.md`; Step 5/6 after |
| [[01b-customers-filter-sheet\|`01b` Customers filter sheet]] (sub-spec) | — | 2026-09-04 | with parent | — | with parent | — | — | Session held 2026-09-04 (O‑2 = newest sales order, all time). **Superseded 2026-09-06** by decisions 52–55: chips + sort menu + header reset replace the sheet |
| [[02-daily-summary\|Daily Summary]] (common: dealer + customer) | S (API) / M (app) | **2026-09-15** (rounds 1–5 the same day; A‑1…A‑8 answered) | **building** — "start API for daily-summary" 2026-09-15; branch `api_screen_daily_summary` from `9af063a` (stacked on #40) in worktree `v1_79/.worktrees/api_screen_daily_summary`; uncommitted until the user says commit | — | #52 (draft, opened 2026-09-15, stacked on #51 on `app_screen_daily_summary`) — **building in parallel since 2026-09-16** (agent team; detached worktree `v1_79/.worktrees/app_screen_daily_summary`; uncommitted) | — | — | Step 0 done 2026-09-15 (spec opened from the template with §1 read from the code). **Step 1 round 1, 2026-09-15:** the user's brief (G‑1…G‑6: Purchase/Sales tabs, start→end date-time window, cursor pages of 12 with infinite scroll, server totals over the whole window, selection switches the card to the selected rows' totals, expandable rows) written into §2/§3/§5/§6. **Round 2, same day:** the user's two frames (`designs/daily-summary/01-portrait.png`, `02-landscape.png`) read as layout D‑1…D‑6 — tabs in the header's right slot, From→To pills + refresh in the range row, a four-number gradient card (Petrol L · Diesel L · Prod. Amt · Cash Reim.), select-all check + status chips + Expand row, running index outside the card, landscape = the Customers panel cell; **user rule: frames are reference only, states as in the code** → v1's chips per tab (Purchase: Pending/Processing/Delivered/Invoiced, default Pending+Processing; Sales: Delivered/Invoiced, default Delivered; last chip stays on) applied on the SERVER with the card following them. Proposals P‑1…P‑13 (one read model per tab; role picks the side, body carries only `companyId`; default window = server IST day echoed in `meta.range`; `on_dt` desc keyset; selection/expansion keyed on the request key; expanding is free; card selection mode = pure client sum of row fields; refresh never `replace`s; two registry keys over `src/screens/v2/Common/DailySummary/`, toggles `screen_v2_<role>_dailysummary`; panel rule lifted from Customers/layout.js to the theme). **Round 3:** user rules "colours from the app's theme, not the screenshot" + "take care of theme, hindi fonts, enlarged font size and bold text" → spec §2c (palette-role mapping: flat `primary` card, `success`/`link` litres, `accent` wash pill; dark from the same roles; Hindi via `useStrings`/`headerTitle`; P‑14 width-driven fallbacks per element at 1.353/2.143; Bold Text = device-only check). **Round 4 (user's answers):** A‑1 Sales · A‑2 v1 chip defaults, a chip tap = a server request · A‑3 no range limit, all IST, latest first · A‑4 n/m from the server (n = selected) · A‑5 folder right + tenancy checked (v3's two order routes have NO bearer check and filter on body ids; v4 = protect → company gate → role → `scopeFilter` + `assertRelation`, table in §5.1) · A‑7 new `DateTimeRange` (iOS one datetime sheet, Android date→time dialogs, `timeZoneName` Asia/Kolkata, one request per commit); **Round 5:** A‑6 = none (as recommended); A‑8 REVISED on the user's reasoning (scrolling never changes totals; selecting changes n + amounts on the phone; filters change both) → summary on every list-defining request (no `cursor`), absent on load-more pages — user: "agreed with suggestion"; **"spec agreed" 2026-09-15** + O‑1…O‑5 (seed depth, `order_msts` index, tile parity, expanded body, card in selection mode). Step 2 dispatched 2026-09-15; O‑3 corrected at dispatch (the tile's `getPtSum` rounds the day's sum, v3 `prodAmt` rounds per line → integer-paise rule per line here, tile parity exact on paisa-clean data, user 2026-09-15: "round every line than sum it" → one rule everywhere; Customers tile adopts it as decision 81 = Step G, a separate pair on this branch). Next = builder report → user reviews → "commit" → PR base `api_screen_customers` |
