# Plan: App screen redesign on a new `api_v4` — the plan of the plan

> **Repos under change:** `dzzlo_oms_app` (new screens, one at a time) and `dzzlo_oms_api` (new `api_v4/` layer). `dip-web` is untouched until a v4 read model is useful to it — with one small exception: the screen-toggle control on the superadmin DB-Actions page (D10).
> **Status:** Plan drafted 2026-09-03. Decisions D1–D4 and D10 confirmed by the user, and "non-money screens first" decided, in the drafting sessions (see §Decisions, §Decision log). **No repo code changes until the user says "start" for a named phase or screen** — this is a house rule, not a suggestion.
> **What this is:** the _workflow, strategy and planning process_ for the project. It deliberately plans **no screen**. Each screen gets its own spec when the user opens its discussion session ([[03-per-screen-playbook]]).
> **Companions:** [[01-tdd-workflow]] (how we test), [[02-foundations]] (one-time groundwork in both repos), [[03-per-screen-playbook]] (the repeatable loop), [[templates/screen-spec]] (the per-screen planning artifact).

## Why this project, and why it is bigger than "new screens"

Three changes land together and each depends on the other two:

1. **New screen structure and visuals.** The app's screens will be replaced one at a time by redesigned screens built from Figma frames. Features will evolve during development; discussion sessions are part of the method.
2. **A new, screen-oriented API.** The current v3 surface forces the app to assemble each screen from several generic endpoints: the Accounts screen awaits three independent calls in series, the invoice detail chains three requests and is cloned in four files, `Customer/NewOrder` orchestrates five endpoints through ~15 effects, and 100 of the app's 132 endpoints are modelled as RTK mutations, so ledger and balance reads get no cache at all. The redesign needs read models shaped for screens and commands with clean contracts. Those are a **new contract**, which by the repo's own rule (`.ai/agents/versioning-agent.md`: "breaking changes always use a new version folder") means a new version folder.
3. **TDD as the method, not a follow-up.** The tasks*12 net protects what exists. Here the contract is being \_designed*, so tests come first by construction: the screen spec produces the failing API tests, the API's fixtures produce the failing screen tests. The full workflow is in [[01-tdd-workflow]].

Doing any one of these alone would be cheaper. Doing them together is what makes the strangler pattern possible: each new screen ships with its own v4 read model and its own tests, while every untouched screen keeps running on v3, and a v1.78 build keeps working throughout.

---

## Decisions

| #   | Decision                    | Choice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Why                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | API versioning              | **`api_v4/` as a thin, additive layer mounted at `/api/v4`.** It owns routes, controllers, read models, commands, schemas and its own middleware chain; it **reuses** `api_v3/services/*`, `models/`, `helpers/`. v3 is frozen for features (test-first bugfixes only). ✅ user-confirmed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | New contract ⇒ new folder (house rule). Reusing services avoids the v2→v3 duplication that `cross_version_edits_plan.md` exists to manage. A full rewrite would delay the first screen by months.                                                                                                                                                                                              |
| D2  | Shape of v4                 | **One read model per screen** (`/api/v4/screens/<slug>`) plus **commands** (`POST /api/v4/<resource>/<verb>`) only where v3 lacks the behaviour or its contract is defective (reads issued as `PUT`, unbounded lists, two-call screens like `so_msts/app/section1`+`section2`). GET for read models keyed by ≤ 3 scalar ids + cursor; POST when the body carries filters.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Screen-shaped payloads are the whole point; commands stay few so business logic keeps one home (the v3 service).                                                                                                                                                                                                                                                                               |
| D3  | Rollout                     | **Strangler, screen by screen.** The app talks to v3 and v4 at the same time; each release migrates a few screens; the version gate retires v3 routes only when no admitted app version can call them. ✅ user-confirmed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Zero big-bang risk; every release is shippable.                                                                                                                                                                                                                                                                                                                                                |
| D4  | Design source               | **Figma frames exist (as images); features evolve in discussion sessions.** Frames go in `designs/<screen-slug>/`; a live Figma URL can be read through the Figma MCP. ✅ user-confirmed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | The spec template's §4 captures the frames and the dated session decisions.                                                                                                                                                                                                                                                                                                                    |
| D5  | Auth posture of v4          | **Every v4 route sits behind `protect` + `authorize(...)` at the router level; the tenant is derived from the token (`req.loggedInUser.co_id` validated by `check_user_company_status`), never from the body; every id the body accepts is membership-checked before sub-queries.** A harness test asserts no v4 route answers without a bearer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | v3 imports `protect`/`authorize`/`scope` and applies them to zero routes; `dealer_id`/`cust_id` come from the client. v4 must not inherit that. (The v3 hole itself is a separate security task — §Open questions.)                                                                                                                                                                            |
| D6  | Envelope                    | Success `{ success: true, data, page?, meta }`; error `{ success: false, error, error_code }`. Cursor pagination everywhere a list can grow: default 25, max 100, opaque cursor, `limit+1` probe. **No unbounded lists in v4.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Keeping `success`/`error_code` means the app's existing error plumbing (`rtkQueryErrorLogger`, `errorRTK`) works unchanged for v4; v3's `limit=0 ⇒ whole collection` default is designed out.                                                                                                                                                                                                  |
| D7  | Validation                  | Schema per route (unknown keys rejected at every level, ObjectId format checked, query values coerced) via a **house validator** in `api_v4/lib/validate.js` — a ~100–150-line schema DSL with no third-party library. ✅ user-confirmed 2026-09-03. When a shape the DSL cannot express appears, the DSL is extended test-first; a dependency is not added.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The API has no input validation anywhere; `sanitizeMongo` strips `$` but not garbage. A library adds no runtime efficiency, only developer time, and the team prefers to own its stack (same reasoning as D10).                                                                                                                                                                                |
| D8  | v4 tests                    | `test/api_v4/` reuses the in-process app (`test/dzzlo_oms_test.js`), `beforeAllHelper`, the seed and the memory-mongod harness; its own contract set `fixtures/api_v4/` with captures, meta, drift detector, freshness gate, and app pull.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Same idiom, same certification (`yarn test:full`), no second harness.                                                                                                                                                                                                                                                                                                                          |
| D9  | App client                  | **One RTK Query instance.** v4 endpoints use an absolute URL built from a new `API_VERSION_PATH_V4` env (all five env files, because `react-native-dotenv` runs with `allowUndefined:false`); tag types are shared so a v3 mutation can still invalidate a v4 read model during the transition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Tags do not cross `createApi` instances; a second instance would break invalidation between old and new screens.                                                                                                                                                                                                                                                                               |
| D10 | Screen location and cutover | New screens live in `src/screens/v2/<Role>/<Name>/` and new shared UI in `src/components/v2/`; route names do not change; a small **screen registry** maps a role-qualified route key (`Dealer/Customers`) to its v1/v2 component — `Orders`, `Invoices`, `Payments` exist as route names in both role trees, so the key must carry the role (clarified 2026-09-03). Kill-switch: **house feature toggles, managed from the superadmin website — no Firebase Remote Config.** ✅ user-confirmed 2026-09-03. One toggle per screen, `screen_v2_<role>_<slug>` (e.g. `screen_v2_dealer_customers`), stored server-side the way the DB-driven version gate stores its config (a `counters` doc, 60-second in-process cache, `GET/POST /sadmin/all/<name>` edited from the dip-web DB-Actions page — see local branch `feature/db-driven-version-gate`); the app reads all toggles in one call, `GET /api/v4/app/features`, after login and on every foreground, and the registry resolves v2 only when the toggle is on. **Default on** when the doc has no entry, the fetch fails, or nothing has been fetched yet — a toggle can only ever _turn off_ a shipped screen, never block boot. Old component deleted the release after the new one ships. The existing `initRemoteConfig({})` call at boot has no consumers; removing it and the dependency is a separate, gated cleanup. | Registry keeps the six navigator files from growing; a server-side toggle backs out a bad screen without store review, exactly like Remote Config would, but stays in the stack the team already owns and operates (same page the superadmin uses for the version gate). Because the read sits behind the bearer, per-company or per-role targeting is possible later without a new mechanism. |
| D11 | Visual system               | The unstarted **tasks_04 design-system plan** (tokens, MD3 typography, semantic colours, font-scale discipline) is executed as foundation step F-APP-3, **scoped to `src/theme/` and v2 screens only**. Old screens are never restyled; tasks_04's screen-migration phase is replaced by this project's per-screen loop. `@shopify/restyle` is re-validated against RN 0.84 / React 19 before install; fallback is tokens + Paper MD3 theme + `AppText`/`Box` primitives.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Two visual systems co-exist for the life of the strangler; that is acceptable because screens are replaced whole.                                                                                                                                                                                                                                                                              |
| D12 | Screen tests                | Tier 3 (RNTL) tests are **decision-only** and red-first; layout is reviewed in the design session against the frames, not asserted by Jest. `src/test/testUtils.js` (`renderScreen`) is created once in F-APP-2.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Matches the tasks_12 policy and keeps the app suite inside its 2-minute budget.                                                                                                                                                                                                                                                                                                                |
| D13 | Touching v3                 | Never for the redesign. If a new screen needs a v3 service to change, that is its own test-first PR with a flow-map row, because it changes behaviour for v1.78 too.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Version safety first (house rule).                                                                                                                                                                                                                                                                                                                                                             |
| D14 | Roles                       | User decides, approves, says "start", ships. Fable facilitates sessions, writes specs and briefs, reviews red commits. Opus execution subagents do red → green → refactor inside a brief.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Standing directive: Fable plans, Opus executes.                                                                                                                                                                                                                                                                                                                                                |
| D15 | Prerequisite                | Merge the three tasks_12 PRs first (API #35, app #48, web #23), re-export fixtures. **Done 2026-09-03** — into `slave` / `slave_dev` (see §Phase 0).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Without them the integration branches have no CI, no gate, no fixtures. The user's PRs target `slave` (API, app) and `slave_dev` (web); `master`/`main` move at release merges only.                                                                                                                                                                                                                                                                                         |

---

## Current state

Surveyed 2026-09-03 — the facts that shape the foundations.

### `dzzlo_oms_api` (surveyed on `api_tdd`; since Phase 0 the same tree is `slave` @ `2ca0301` = `api_v4_foundations`)

- **PR #35 merged into `slave` on 2026-09-03** as a fast-forward (master's ledger work was already in). `slave` = `master` + 12 until the next release merge. Fixtures re-exported on seed `v3_2026-09-03` (`9d5c61e`); gate PASS.
- Mount chain in `dzzlo_oms.js`: `compression → express.json(1mb) → morgan(dev) → /healthcheck → api_key_v1() → logging() (sets req.loggedInUser from the Bearer) → check_user_version() → cookieParser → sanitizeMongo → helmet → cors → static → app.use("/api/v2") → app.use("/api/v3") → app.use("/api/dip/v1") → errorHandler`. v1 mount commented out. Global rate limit commented out.
- `api_v/api3.js` order: `/contact`, `/auth` **before** `api_key_v3()`; then `legacy_credit_presenter()`; `/sadmin`, `/cust_msts`, `/dealer_msts`, `/invites` **before** `check_user_company_status()`; everything else after.
- Auth primitives in `api_v3/auth.js` (`protect`, `authorize`, `scope`, 30-second user cache) are correct and **applied to zero business routes**. Tenant identity = `co_id` on the user; the only server-side derivation in v3 is `controllers/collections/veh_trns.js:40-41` — the precedent v4 generalises.
- `helpers/advancedResults.getResults`: `limit = parseInt(...) || 0` ⇒ a missing `limit` returns the whole filtered collection. `GET /dealer_custs` has no pagination and populates both sides. `getSalesOrdersHeader` (`services/so_msts.js:300-420`) awaits 7+ independent queries in series; `allRelationCurrBal` fans out one balance computation per relation. `multipleOrderRes` (`services/order_msts.js:415`) is the good pattern: two waves of batched `$in` + `Map`.
- Test harness: 49 active suites under `test/api_v3/`, one memory mongod per file, seed via real API calls, `docs/testing.md` (440 lines) is the runbook, `scripts/release_gate.sh` + `check_fixtures_fresh.js`, CI on `test:full`. Jest picks up any `*.test.js` not in the legacy ignore list, so `test/api_v4/` needs no config change (verify in F-API-1).
- No input-validation library, no OpenAPI. Express 5, Mongoose 9. Version gate: hardcoded `check_user_version` (≤ 1.68 blocked) plus nine files with inline `meta.version` branches; a DB-driven gate is in flight on local branch `feature/db-driven-version-gate`.
- Governance files that this plan must amend (approval needed): `AI.md` "Write ONLY inside `api_v3/`" and `.ai/agents/versioning-agent.md` version map.

### `dzzlo_oms_app` (surveyed on `app_tdd`; since Phase 0 the same tree is `slave` @ `a7c32d7` = `app_v4_foundations`, `main` + 10)

- RN 0.84.1, React 19.2.3, react-navigation 7, RTK 2.11, Paper 5.15 (used shallowly: `useTheme`/`Text`/`Divider`; buttons and inputs are hand-rolled), Reanimated 4, FlashList in 13 files vs FlatList in 54, Firebase (`perf` is live in the base query; `remote-config` is a dependency).
- Navigation: `AppNavigatorContainer` → role tree `Drawer → (Tab | Stack) → native-stack`; both `TrnTab.js` files swap in read-only `Common/` variants by user scope. Navigator boilerplate is duplicated in six files.
- 193 screen files / ~92k LOC. Orders, Invoices, Payments each exist three times (Dealer/Customer/Common); the invoice detail exists four times; ten screen files exceed 1,100 lines. Extracted business logic in `src/helpers` is ~300 LOC; money math lives in render.
- Data layer: one `createApi` (`reducerPath 'dzzlo-oms-api'`, `baseUrl = API_URL + API_VERSION_PATH` = `/api/v3` in every env file), retry with 4xx short-circuit, perf wrapper, `makeStore()` factory. 132 endpoints (32 queries / 100 mutations); `fetch_one_dealer_customer` exists twice (25 call sites); every list screen imports both the GET-paginated and the POST-filtered twin.
- Tests: 16 files (Tier 1 + Tier 2), Jest 30 + MSW 2, virtual mocks for the two phantom native packages, **no screen tests, no `src/test/testUtils.js`, no shared MSW handlers file**. Fixtures pulled from `../dzzlo_oms_api/fixtures/api_v3/` only.
- Styling: 3,424 inline style objects and 1,160 literal hex colours; `src/constants/designTokens.js` exists and is unused; theme in `src/utils/Colors/index.js` (`Light`/`Dark`, MD3 `fontObject`). The tasks_04 design-system plan: 1 of 205 tasks checked.

---

## Phases

| Phase                          | Doc                                        | Outcome                                                                                                                                                                                          | Gate to start                                                                                                                                                                                                                        | Effort (guide)                                    |
| ------------------------------ | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- | ------------ |
| **0 — Prerequisites**          | this doc §Phase 0                          | Three TDD PRs merged; fixtures re-exported and pulled; Figma frames in `designs/` (D7 and D10 are decided)                                                                                       | user: "start phase 0"                                                                                                                                                                                                                | ½ day                                             |
| **1 — API foundations**        | [[02-foundations#Phase 1 — API foundations | 02 §Phase 1]]                                                                                                                                                                                    | `/api/v4` mounted with its chain, envelope, validation, tenancy helpers, cursor pagination, compose runner, v4 fixture set, the screen-toggle store + `GET /app/features`, docs — every piece test-first; **no screen endpoint yet** | Phase 0 done; approvals in §Governance granted    | 3–5 dev-days |
| **2 — App foundations**        | [[02-foundations#Phase 2 — App foundations | 02 §Phase 2]]                                                                                                                                                                                    | v4 client, `renderScreen` harness, design tokens + primitives, screen registry + toggle-driven cutover, strings/errors convention, docs — test-first; the dip-web toggle control (F-WEB-1) lands in parallel; **no screen yet**      | Phase 1 merged and on staging                     | 4–6 dev-days |
| **3 — Screens, one at a time** | [[03-per-screen-playbook]]                 | Each screen: spec → API → design → screen → ship → retire                                                                                                                                        | per screen: "spec agreed" then "start …"                                                                                                                                                                                             | S 3–5 days · M 1–2 weeks · L split into sub-specs |
| **4 — Retirement**             | §Phase 4 below                             | v3 routes deprecated then removed behind the version gate; old screens and duplicate endpoints deleted; `legacy_credit_presenter` and inline version branches removed when their versions retire | when every consumer of a route is gone                                                                                                                                                                                               | continuous                                        |

Phases 1 and 2 are sequential (the app foundation's proof is a real `/api/v4` ping against staging). Phase 3 repeats. Phase 4 is folded into each screen's Step 6 and closed out at the end.

### Phase 0 — Prerequisites

- [x] Merge the three tasks_12 PRs — **done 2026-09-03**. The user had retargeted all three to the integration branches beforehand (`slave` for the API and the app, `slave_dev` for dip-web), so `master`/`main` receive them at the next release merge. API #35 → `2ca0301` (fast-forward, no reconciliation needed), app #48 → `a7c32d7`, dip-web #23 → `23ac5b0` (the user had already merged `slave` into `web_tdd`; the missing `istDayEnd` regression test went in first as its own commit `2b5efec` — `src/utils/ledgerCheck.test.js`, 14 cases, teeth-checked). Merge commits, not squashes, so the tdd HEADs remain ancestors of the foundation branches.
- [x] Fixtures — **done 2026-09-03**, committed on the PR branches before merging: API `9d5c61e` (`yarn seed && yarn fixtures:export`, seed `v3_2026-09-03`), app `9bb942f3` and web `5d289a9` (`yarn fixtures:pull`). Gate: `bash dzzlo_oms_api/scripts/release_gate.sh` → **RELEASE GATE: PASS** (fixtures fresh, API 778 passed / 47 skipped / 1 todo, web 46, app 337).
- [x] Decide D7 and D10 — both decided 2026-09-03 (house validator, house screen toggles — see D7, D10).
- [x] Frames for the first screen are in `designs/customers/` (`01-default.png`, `02-filter-sheet.png`, 2026-09-03); each later screen drops its own at Step 1.
- [x] Create branches — done 2026-09-03 at the user's request: `api_v4_foundations` from `api_tdd` @ `ecdd3ae` and `app_v4_foundations` from `app_tdd` @ `8b6831df`; fast-forwarded the same day to the merged `slave` heads (`2ca0301` / `a7c32d7`), so each equals its repo's `slave`. dip-web gets `web_v4_foundations` from `slave_dev` when Phase 2's F-WEB-1 starts. Per-screen branch layout is deferred.

**Phase 0 closed 2026-09-03.** Next gate: the user says "start phase 1".

### Phase 4 — Retirement (rules)

1. A v3 route gets a `Deprecation` header + log line the release after its last app consumer is deleted (Step 6 of the playbook).
2. A v3 route is **removed** only when the version gate no longer admits any build that can call it. Each removal is an explicit, dated decision in this doc's log.
3. `legacy_credit_presenter` and the nine inline `meta.version` branches are removed when the DB-driven version gate lands and the corresponding versions are retired — a separate PR each, test-first.
4. The `Common/` read-only twins and the four invoice-detail clones are deleted as their replacing screens ship; the six navigator files shrink to registry lookups.

---

## Governance

What needs the user's explicit approval before an execution agent may touch it.

| Item                                                                                                                        | Repo    | Why it is gated                                                                                                                                                                                                                                              | Phase |
| --------------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- |
| `api_v/api4.js` (new) + one line in `dzzlo_oms.js` + one line in `test/dzzlo_oms_test.js`                                   | API     | mounting a version is gated by `versioning-agent.md`                                                                                                                                                                                                         | 1     |
| `AI.md` Active Development Rule and `.ai/agents/versioning-agent.md` version map amended for v4                             | API     | governance docs                                                                                                                                                                                                                                              | 1     |
| `scripts/check_fixtures_fresh.js`, `test/api_v3/temp/seed/export_fixtures.js` generalised for two fixture sets              | API     | shared tooling                                                                                                                                                                                                                                               | 1     |
| `API_VERSION_PATH_V4` added to `.env.development/.testing/.production/.ci/.example`                                         | App     | `allowUndefined:false` — a missing var breaks the bundle                                                                                                                                                                                                     | 2     |
| `@shopify/restyle` install (only if re-validated)                                                                           | App     | dependency policy; tasks_04 decision to re-confirm                                                                                                                                                                                                           | 2     |
| `GET/POST /sadmin/all/app_features` in `api_v3/routes/sadmin/index.js` + controller (write side of the screen toggles, D10) | API     | v3 is frozen (D13); this is the one **additive** exception — a superadmin route with no v1.78 behaviour change, same shape as the `version_gate` routes on `feature/db-driven-version-gate` (a v4 `/sadmin` route instead would force dip-web onto v4 early) | 1     |
| Toggle list on `src/pages/superadmin/db/ImpActions.js` (+ `store/apis/sadmin/imp_actions.js`)                               | dip-web | dip-web is otherwise out of scope; one small test-first PR                                                                                                                                                                                                   | 2     |
| Any edit under `api_v3/services/*`, `models/*`, `helpers/*`                                                                 | API     | changes v1.78 behaviour; test-first PR with flow-map row, one per change                                                                                                                                                                                     | 3     |
| Any `git rm` (old screens, legacy suites)                                                                                   | both    | deletions are always separately gated                                                                                                                                                                                                                        | 3–4   |

Everything else in Phases 1–2 is additive inside `api_v4/`, `test/api_v4/`, `fixtures/api_v4/`, `src/screens/v2/`, `src/components/v2/`, `src/theme/`, `src/store/apis/v4/`, `src/test/`.

---

## Risks

| Risk                                                          | Detail                                                                                                                                                 | Mitigation                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🔴 v4 inherits v3 service defects                             | A read model that calls `getSalesOrdersHeader` inherits its seven serial awaits; one that calls `allRelationCurrBal` inherits the per-relation fan-out | Read models compose the _lower_ service pieces (queries, presenters like `multipleOrderRes`), not the top-level handlers; a serial-await fix inside a v3 service is a separate test-first perf PR (D13)                                                                     |
| 🔴 Two API versions in flight for months                      | Every ledger rule now has one home (v3 service) but two callers                                                                                        | v4 has no business logic of its own (commands compose services); the drift detector and flow map cover both; v3 frozen for features                                                                                                                                         |
| 🟠 Scope creep in sessions                                    | "Features will evolve"                                                                                                                                 | Only sessions grow scope; every change is spec → test → code in one PR (01 §1.4); `it.todo` never merges                                                                                                                                                                    |
| 🟠 Design fidelity vs test fidelity confusion                 | Nobody asserts pixels, so a screen can be green and wrong                                                                                              | Design session 2 reviews the built screen against the frames with the screenshot checklist before merge                                                                                                                                                                     |
| 🟠 Fixture staleness across two sets                          | v3 and v4 metas, two pull targets                                                                                                                      | freshness gate covers both; release gate blocks                                                                                                                                                                                                                             |
| 🟡 Screen toggles add a boot-time fetch                       | `GET /app/features` fails or is slow ⇒ must default to v2, never block; a toggle flipped off must reach devices without a deploy                       | fetch is fire-and-forget after login and on foreground, navigator reads the store synchronously; default-on for unknown/unfetched keys; last-known values survive a failed refresh; server cache 60 s (version-gate precedent); unit tests pin the default and the off-path |
| 🟡 Pre-existing v3 tenant-isolation hole becomes more visible | v4 proves tenancy per route; v3 still does not                                                                                                         | schedule the v3 security task separately; do not widen the hole (no new v3 routes)                                                                                                                                                                                          |
| 🟡 Suite budgets                                              | app ≤ 2 min, API `test:full` ≤ 5 min                                                                                                                   | Tier 3 decision-only; per-file mongod already cut the API suite to ~26 s                                                                                                                                                                                                    |

---

## Open questions for the user

1. ~~**Kill-switch (D10):** Firebase Remote Config flag per screen or release-only cutover?~~ ✅ **Decided 2026-09-03:** neither — house feature toggles managed from the superadmin website (see D10). Numbering kept so cross-references stay valid.
2. ~~**Validation library (D7):** zod or a house validator?~~ ✅ **Decided 2026-09-03:** house validator in `api_v4/lib/validate.js`, no new dependency (see D7, [[02-foundations#F-API-2 — Envelope, errors, validation|02 §F-API-2]]).
3. ~~**First screen posture:** start with a non-money screen?~~ ✅ **Decided 2026-09-03:** yes — the first screens are non-money screens, so the loop proves the method before any ledger command is involved. Candidate list is drawn up in the first screen session, not here.
4. **v3 security task:** schedule the tenant-isolation fix for v3 as its own tasks_NN (recommended, independent of this project)?
5. **Figma:** images only, or is there a live file whose frames can be pulled through the Figma MCP?
6. **dip-web:** stays out of scope until a v4 read model is useful to it — confirm.

---

## Decision log

| Date       | Decision                                                                                                                                                                                                                                                    | By   |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 2026-09-03 | D1 (api_v4 thin layer), D3 (strangler), D4 (Figma images + sessions), scope = new structure + visuals                                                                                                                                                       | user |
| 2026-09-03 | Plan the plan only; screens are planned one at a time in their own sessions                                                                                                                                                                                 | user |
| 2026-09-03 | D10 kill-switch: **no Firebase Remote Config**; house feature toggles per screen, managed from the superadmin website (dip-web DB-Actions page), stored server-side on the version-gate pattern, read by the app via `GET /api/v4/app/features`, default on | user |
| 2026-09-03 | First screens are **non-money** screens (open question 3)                                                                                                                                                                                                   | user |
| 2026-09-03 | D7 validation: **house validator** (`api_v4/lib/validate.js` schema DSL), no third-party library; the zod approval row is withdrawn                                                                                                                         | user |
| 2026-09-03 | **First screen = Customers (dealer)**, size S — Step 1 session held, 14 decisions in [[screens/01-customers#4. Design references ◌\|spec §4]]; sequencing stays foundations-first (Phases 0–2 before the screen build)                                          | user |
| 2026-09-03 | v4 lists: **12 rows per page** with infinite scroll for the Customers screen; all sorts, filters and search (name only) server-side; per-user-per-company list preferences via a v4 command — storage proposed as a new additive `user_prefs` collection instead of `models/users.js` (user asked; Fable recommended; confirmation pending) | user |
| 2026-09-03 | D10 location carries the role: `src/screens/v2/<Role>/<Name>/`, registry key `<Role>/<Route>`, toggle `screen_v2_<role>_<slug>`; Customers → `src/screens/v2/Dealer/Customers/`, `screen_v2_dealer_customers`. Prefs storage: `user_prefs` collection, one doc per user × company, `screens.<role>_<slug>` keys validated by the v4 route schema (structure in spec §5.2) | user |
| 2026-09-03 | `DR` tag = discount on a product whose PRIMARY category is Diesel; largest live dealer = 130 relations, so a per-request pass over all relations is acceptable | user |
| 2026-09-03 | Customers: page size is a **constant 12 on the API** (no `limit` key on that endpoint — a per-spec exception to D6's default-25/max-100); summary count = all non-hidden relations, but **daily total and total balance follow the active filter and search**; every remaining assumption confirmed ("rest is all ok"); spec complete, awaiting "spec agreed" | user |
| 2026-09-03 | Customers: `hidden` = the Blacklist switch (shown when the filter passes, A‑8); `closed` is a dead field, dropped; filter frame read → three **tri-state** status toggles (Verified · Has Trans. · Blacklisted) and six sorts (Creation Date default · Name · A/c Balance · Daily Total · Credit Total · Latest Order wise — Name added by the user after the frame); "Has Trans." = any SO/invoice/payment this FY (one extra grouped `so_msts` query); "Latest Order wise" definition deferred to the **filter-sheet sub-spec `01b`** (O‑2); row = the Dealers frame's row, no numbered badge | user |
| 2026-09-03 | **Customers spec agreed** (Step 1 closed after eight rounds; `name` sort re-added; O‑2 "Latest Order wise" deferred to sub-spec `01b` by agreement). Next gate: "start phase 0" | user |
| 2026-09-03 | Customers credit sheet renders from **row data** — one endpoint per screen ("if we are efficient with single endpoint we will go with it"); the fresh-credit endpoint proposed earlier is withdrawn; no refresh button in the sheet, an "Updated HH:MM" footer from `meta.generatedAt` proposed instead (A‑7) | user |
| 2026-09-03 | Design source for Customers: the customer-side "Dealers" frame (image); the dealer screen mirrors it with no Add button. The customer-side Dealers screen is a later screen                                                                                    | user |
| 2026-09-03 | Branches `api_v4_foundations` / `app_v4_foundations` created from the tdd HEADs (Phase 0 item); screen-branch layout deferred                                                                                                                                 | user |
| 2026-09-03 | **Phase 0 done** ("start phase 0"): the tasks_12 PRs merged into the integration branches the user had retargeted them to — `slave` (API #35 `2ca0301`, app #48 `a7c32d7`) and `slave_dev` (dip-web #23 `23ac5b0`), not `master`/`main`; fixtures re-exported on seed `v3_2026-09-03` and pulled into both front-ends; the missing `istDayEnd` regression test added in dip-web (`2b5efec`); release gate PASS; foundation branches fast-forwarded to the merged heads. Next gate: "start phase 1" | user |
| 2026-09-04 | **"start phase 1"** given. Read as approval of the Phase-1 governance rows (`api_v/api4.js` + the two mount lines; `AI.md` / versioning-agent amendments; exporter + freshness script generalised; the two additive `/sadmin/all/app_features` routes) — the phase cannot be built without them. Execution: sequential Opus agents on `api_v4_foundations`, red/green commit pairs, PR → `slave`. Staging deploy of `/api/v4/ping` and `/app/features` stays the user's step | user |
| 2026-09-04 | F-API-1/2 built (4 commits, 143 tests, `test:full` 921 green). **Body dates:** ISO-8601 strings, validated by an explicit `isoDate` DSL rule added test-first when a spec first needs one; `type: "date"` stays query/params-only, body coercion stays off | Fable |
| 2026-09-04 | F-API-3/4/5 built (7 commits, +164 tests, `test:full` 1085 green); draft PR #39 → `slave` opened so CI runs per push (the workflow triggers only on PRs and `master` pushes). Seed facts recorded in 02 §Phase 1 | Fable |
| 2026-09-04 | F-API-8 and F-API-6 built (5 commits, +66 tests, `test:full` 1151 green): toggle store on the `counters` pattern with merge-write semantics, `fixtures/api_v4/` set + two-set freshness gate. `helpers/appFeatures.js` is a new additive helper (versionGate/dieselQtyLimit precedent) — `AI.md` wording to say so in F-API-7 | Fable |
| 2026-09-04 | **Phase 1 built** — F-API-7 docs/governance landed; PR #39 (`api_v4_foundations` → `slave`, 18 commits, 373 tests) marked ready for review; gate PASS with two fixture sets. Waits on the user: merge + staging check. Next gate: "start phase 2" (app foundations on `app_v4_foundations`, PR → `slave`; F-WEB-1 on a new `web_v4_foundations` → `slave_dev`) | Fable |
| 2026-09-04 | **"start phase 2"** given (PR #39 still open — Phase 2 pulls `fixtures/api_v4/` from the sibling checkout on `api_v4_foundations`). Read as approval of the Phase-2 governance rows (`API_VERSION_PATH_V4` in the five env files; the dip-web toggle page). restyle rejected (02 §log). **User rule, same day: "do not merge any pr"** — every PR is opened and marked ready, the user merges. Execution: app agents sequential on `app_v4_foundations` (PR → `slave`), dip-web F-WEB-1 in parallel on `web_v4_foundations` (PR → `slave_dev`) | user |
| 2026-09-04 | F-WEB-1 built: dip-web PR #27 → `slave_dev` (open, CI green). Flip = per-key POST (merge), plus an "Add toggle" row for never-written keys | Fable |
| 2026-09-04 | Session cut mid Phase 2 (user: system going to sleep). App `app_v4_foundations`: F-APP-1 + F-APP-2 committed (4 commits, unpushed, no PR yet), F-APP-5 half-written (untracked test file), F-APP-3/4/6 not started; the execution agent was interrupted, so the tree must be re-verified (`git status`, `yarn test`) before resuming. Resume plan recorded in memory | Fable |
| 2026-09-04 | Resumed ("continue"): F-APP-5 finished, F-APP-3 and F-APP-4 built (10 commits on `app_v4_foundations`, `yarn test` 626 in 3 s); draft app PR #49 → `slave` opened for CI; F-APP-6 + certification dispatched | Fable |
| 2026-09-04 | **Phase 2 built** — app PR #49 → `slave` ready (11 commits, 626 tests, gate PASS with three fresh fixture contracts); dip-web PR #27 → `slave_dev` ready. Nothing merged (user rule). Waits on the user: merge #39 → #27 → #49, device checks. Next: `01b` filter-sheet session, then "start API for customers" | Fable |
| 2026-09-04 | **`01b` filter-sheet session — O‑2 closed:** "Latest Order wise" = newest sales order (`so_msts.on_dt`) per customer, **all time**, from the grouped query Has Trans. already needs (FY count becomes a conditional sum; zero extra round trips); no-order customers last both ways. Also: the frame's "at least one required" note removed; Name sort = "Customer Name", second. Parent §5.1 updated, the last `it.todo` replaced by five tests. Customers is now fully specified — next gate: **"start API for customers"** (after PRs #39/#27/#49 merge) | user |
| 2026-09-04 | **"start API for customers"** given while PR #39 is still open → branch `api_screen_customers` stacks on `api_v4_foundations`, PR → `slave`. Read as approval of `models/user_prefs.js` (additive, spec §5.2) and of **export-only lines** in `api_v3/services/dealer_custs.js` for the four private helpers the read model must reuse (`getCsSum`, `getPtSum`, `pendingPOlist`, `uninvoicedSOlist`) — zero logic change, so not a D13 behaviour edit. The credit-utilisation ratio (v3 has it inline in `order_msts.js:795–806`, not exported) is a pure function in the read model with a parity test against v3's rule | user |
| 2026-09-04 | **Customers API built** — PR #40 → `slave` (stacked on #39): read model + prefs command, +177 tests, `test:full` 1328, 11 round trips pinned. Builder clarifications ratified in spec §4 (39–41: all-or-nothing body override, prefs `updatedAt` refresh on replay, `cursor: null` = 400). **O‑3 opened:** seed enrichment (test-only) needed before the app's Tier 3 tests | Fable |
| 2026-09-04 | **"start screen customers"** given with PRs #39/#27/#49/#40 still open → app branch `app_screen_customers` stacks on `app_v4_foundations`, PR → `slave`. **Step 3 (design session) skipped by the user's gate**: 01b A‑1…A‑4 stand as written, P‑1 (row "Last order …" line) not adopted, design fidelity reviewed on device by the user at Step 5. **O‑3 resolved without a seed change:** the API exporter gains scenario captures that mutate the in-memory seed before capturing (blacklisted + unverified row, capped credit + Diesel discount row, 13 relations → page 1/page 2, a body-sourced and a saved-pref response); `page.next` joins the scrub's volatile keys. v3 suites untouched | user |
| 2026-09-04 | Screen build in flight: API PR #40 gained the scenario captures (`page.next` scrubbed only when it is a cursor string — v3 keeps its `pagination.next` numbers; suite 1335); app branch `app_screen_customers` = Tier 1 (105 tests) + Tier 2 (endpoints with cursor-keyed cache, 23 tests, fixtures pulled) → draft **app PR #50** → `slave` (stacked on #49); Tier 3 screen + cutover and the docs/PR-body run as **parallel agents** (user: "use agent teams to make things fast" — docs agent in an isolated worktree, cherry-picked afterwards) | user |
| 2026-09-04 | **Customers screen built** (Tier 3 + cutover, 16 decision tests, suite 770; API `_states` capture gained a BLOCKED line). Close-out agent: cherry-pick the docs commit, re-pull, pin BLOCKED, gate, PR body → app PR #50 ready. Harness facts recorded in spec §4 for later screens | Fable |
| 2026-09-04 | **Customers screen closed out — app PR #50 ready** (17 decision tests incl. the BLOCKED line from the re-exported `_states` capture; docs `docs/screens/customers-v2.md` + testing-guide worked example; suite 771 in 4.8 s; release gate PASS with all three contracts fresh). Steps 1–4 of the playbook done for screen 01; Step 5 (ship) is the user's: merge #39 → #27 → #49 → #40 → #50, staging + device checklist, then the 7-day window; Step 6 deletes the v1 screen, `RelationFilterBS.js` **and** `Balance/Components.js` (only imported by v1 Customers). **Follow-ups found:** (a) API v3 suite flake — `api_v3/services/order_msts.js:54` `getTransactionalSMSBalance` makes a live call to `2factor.in` inside a test run and kills `collections/order_msts/index.test.js` when the site answers HTML (2 of 4 gate runs); needs a test-first fix (mock/guard) in the API repo; (b) `src/theme/provider/useAppTheme.js` error hint wrongly says `renderScreen()` provides the theme | Fable |
| 2026-09-04 | Screen 01 design session (Step 3) held after Step 4 on the simulator build: three rounds on a design canvas (Touch-ups / Statement → Ledger → chevron + ring-avatar + text tags → caption header card), decisions 42–50 in the spec §4; restyle built the same day as app PR #51 (stacked on #50; `Screen` gains `edges`; 836 tests, CI green), ready, not merged. Playbook note: a design session after a first build is cheap when the row and screen are componentised — the restyle was five red→green pairs by parallel builders. | user + Fable |
| 2026-09-06 | Screen 01 design corrections on the simulator build of PR #51: nine canvas rounds (decisions 51–58) — sort menu (Paper `Menu`) replaces the filter sheet, two tri-state chips + a direction chip, reset behind a confirmation in the header slot, 56 pt avatar with disabled/error tones, chip on the name line, full-width dividers; `01b` superseded. Built the same day as two red→green pairs (40 suites / 837 tests). Lesson: a review on the running build after the first restyle found nine changes the canvas rounds had not — keep the simulator pass in the playbook before "done". | user + Fable |

---

## References

- House TDD guides: [app](../tdd-testing-guide.md) · [API](../../oms_api/tdd-testing-guide.md) · tasks_12 design record `../../tasks/tasks_12_tdd_testing/`
- Prior art absorbed here: `../../tasks/tasks_02_major/03-bff-composite-endpoints.md` (BFF conventions, security requirements §11), `04-cursor-pagination-infinite-scroll.md`; `../../tasks/tasks_04_design-system/` (tokens, typography, colours, clipping); `../../learning/api-pattern-problems/` (perf roadmap); `../../tasks/tasks_13_db_architecture/` (financial write-path plan — independent, but v4 commands must not pre-empt its transaction work)
- API repo governance: `AI.md`, `.ai/agents/versioning-agent.md`, `docs/strategy/api_v3_refactor_plan.md` (how v3 was introduced — v4 follows the same mount pattern), `SEAMS.md`, `docs/bug-museum.md`
