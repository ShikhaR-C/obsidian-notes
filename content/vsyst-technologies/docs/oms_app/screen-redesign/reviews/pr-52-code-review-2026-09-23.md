# Code review — app PR #52 (`app_screen_daily_summary` → `app_screen_customers_design`)

> **What this is:** the review the user asked for on **2026-09-23** — "also code-review PR #52 and #51 use agent teams". PR: <https://github.com/ShikhaR-C/dzzlo_oms_app/pull/52> — *feat(v2): Daily Summary screen — the second redesigned screen (screen 02, stacked on #51)*. Its spec is [[screens/02-daily-summary]].
> **Verdict:** **the screen is sound; its paging has three real defects, all still live at the top of the stack** (`dcde2c8b`, 2026-09-23): a tag invalidation deep in the list appends page N instead of refreshing page 1 and the summary; a pull-to-refresh while a load-more is in flight is silently dropped; a failed load-more can never be retried by scrolling. Two more live ones: the selection is pruned past the 500-row cache cap, and the driver line prints "undefined" when a record lacks a name or phone. **Four findings are gone by #53** (`47057bbc`: the date buttons' device clock, DTBS's literals, DTBS's modals in the overlay, `sentTo`), **one by #54** (`f09d7b5b`: the panel's refresh control), **two in part** (the product figures by #54's tail; `DateTimeRange`'s parser copy by #53). Eight of fifteen are open at `dcde2c8b`.
> **Source:** head **`f44348fc`**, base `d0fbd081` (`app_screen_customers_design`, PR #51's head) — **23 commits, 112 files, +18,532 / −583**. Suite at head **100 suites / 2,281 tests green, 15.8 s** (scratch worktree, `.env.ci`). ESLint over the touched files: three errors, all in v1's `src/components/DatePicker/DTBS.js:383–385` (`react-hooks/exhaustive-deps`), **all present on the base** — the PR touched the file without adding them; the rest are the navigators' long-standing `no-unstable-nested-components` warnings. Every `file:line` below is from `f44348fc`; **Now** says whether it is still there at `dcde2c8b`.
> **How:** the `code-review` skill at **high**, one forked agent (it dropped its own `Date`-in-args candidate after confirming `serializableCheck` is off); the orchestrator re-read findings 1–4, 6, 9 and 15 in the source at `f44348fc` and at `dcde2c8b`, and read RTK Query 2.11.2's thunk `condition` (`rtk-query.modern.mjs:1140`: an in-flight cache key is never re-dispatched) for finding 2.
> **Companions:** [[screens/02-daily-summary]], [[08-v2-optimisation-plan]], [[reviews/pr-53-code-review-2026-09-23]] (on top of this: the sheet that retired the date buttons), [[reviews/pr-54-code-review-2026-09-23]], [[reviews/pr-51-code-review-2026-09-23]] (under it).

---

## 0. The verdict in one table

| | |
| --- | --- |
| **Defects, live now** | invalidation with a cursor **appends** (`daily_summary.js:77`) · refresh during a load-more is dropped (`useScreenModel.js:308`) · a failed load-more cannot be retried (`:294`, `:368`) · the prune effect evicts selected ids past `MAX_CACHED_ORDERS` (`:236`) · the driver line's `undefined` (`OrderDetails.js:91`) · `SummaryCard`'s 0.6 `minimumFontScale` is a font cap by another name (`:128`; the same class as #54's finding 1) |
| **Cleanup, live now** | three prune variants for one rule (`pruneSelection`, `pruneSet`, the model's `pruned`) |
| **Gone by #53** (`47057bbc`) | the From / To buttons' device clock · DTBS's English-only literals · DTBS's four modals in the quick-return overlay (the block itself is still drawn twice) · `sentTo`'s magic 23:59:59.999 · `DateTimeRange`'s parser copy (`isoOf` in `request.js` remains) |
| **Gone by #54** | the panel's copied refresh control (`f09d7b5b`) · the product amounts as one-line figures (`a05d0220`; the cash line still wraps) |
| **Held** | the list's cache key (body minus cursor), de-dup by `id`, the summary kept from page 1, the v3 tag sharing with v1, the P‑6 prune on a refresh, the chips and tabs, the collapsing chrome |

## 1. Findings, most severe first

| # | Where (`f44348fc`) | What | When it bites | Fix | Checked | Resolved? (#53 `44eb266e` · #54 `dcde2c8b`) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `src/store/apis/v4/daily_summary.js:77` (`merge`) | `if (!arg?.cursor) return incoming;` else append. A tag invalidation re-runs the **subscription's current args**, which carry a cursor after a load-more, so the refetch of page N is appended / de-duped and `page` / `meta` become page N's — page 1's rows and the card's `summary` are never refreshed. The docstring ("a tag invalidation … re-request[s] page 1") is false | Scrolled to page 3 → a v1 screen underneath fires `update_order_msts_status` (invalidates `order_msts_POST / LIST`) → the moved order stays in its old state on screen and in the totals | Keep the hook subscribed on page-1 args and fetch further pages with `initiate({ …, cursor }, { subscribe: false })` — same cache key, `merge` still appends, and every invalidation re-requests page 1 and replaces | orchestrator, in source (the merge and the docstring) | **open in both** — `daily_summary.js:77` |
| 2 | `src/screens/v2/Common/DailySummary/useScreenModel.js:308–315` (`refresh`) | Deep in the list `refresh` drops the cursor and returns. The page-1 args share the cache key with the pending page N, and RTK Query's thunk `condition` refuses to dispatch while that key is `pending` — so **no page-1 request is made**; page N lands and is appended; `isRefreshing` (`isFetching && !cursor`) showed a spinner for a refresh that never happened. `loadMore` guards on `isFetching`; `refresh` does not | Footer spinner up (page 2 pending) → pull to refresh → nothing, then page 2 appends | Guard like `loadMore`, or remember the pull and run it on the pending fulfilment; or abort the in-flight page first | orchestrator: the code, and RTK Query 2.11.2's `condition` | **open in both** — `refresh` has no `isFetching` guard at either head (`:266–274` at `dcde2c8b`) |
| 3 | `useScreenModel.js:294–299` (`loadMore`) with `:368` (`error`) | Page N fails → `error` is `null` because `hasData`, `isFetchingMore` is false, nothing is drawn; scrolling to the end again calls `setQuery` with the **same** cursor, which returns `previous`, so nothing is dispatched | A 500 or a timeout on page 2: the list just ends; only a pull-to-refresh (page 1 again) recovers | A footer error state with Retry, and let `loadMore` re-dispatch when the last cursor page failed (compare `page.next` with the last **fulfilled** cursor, not the query's) | orchestrator, in source (`:326` at `dcde2c8b`) | **open in both** — the same-cursor bail at both heads (`:248–254`), `error` nulled on `hasData` (`:326`) |
| 4 | `useScreenModel.js:230–240` (the prune effect) with `daily_summary.js:83–87` (`MAX_CACHED_ORDERS = 500`) | The endpoint evicts the oldest rows past 500; the prune effect runs on **every** fulfilment of the same list (load-more included) and drops the evicted ids from the selection and the expansion set. The comment "after a load-more that is every id (the rows only grew)" is false past the cap | Include mode: tick row 1, scroll ~42 pages → row 1 is spliced out → its id leaves the selection, the card total and "All n/m" drop with no tap. Exclude mode: an un-ticked evicted row re-enters the totals | Prune only after a page-1 fulfilment (no cursor), or keep the selection independent of the loaded rows; 500 rows is small enough that the eviction could go | orchestrator, in source | **open in both** — `pruneMark` and the 500-row splice at both heads |
| 5 | `src/screens/v2/Common/DailySummary/components/OrderDetails.js:91` | `` value={`${row.driver.name} (${row.driver.phone})`} `` with only `row.driver` guarded | `{ name: 'Ramesh' }` prints "Ramesh (undefined)"; a phone alone prints "undefined (98…)" | Build the value from the parts present | orchestrator, in source | **gone in #53** (`47057bbc`): `RangeButtons.js` deleted; the bar prints IST through `helpers/DateRange` |
| 6 | `src/screens/v2/Common/DailySummary/components/SummaryCard.js:128` | `adjustsFontSizeToFit={shrink}` with `minimumFontScale={SUMMARY_MIN_FIGURE_SCALE}` (0.6): a figure past its promise is drawn at 60 % of its scaled size. AI.md: *"Font scaling is never capped … If a layout breaks at 200 %, the layout is wrong."* | At 200 % a total longer than the sample is effectively capped at 1.2×; the `list` / `column` shapes exist to reflow instead | One decision for this and #54's `OrderDetails` figure: reflow, or a written exemption for figures | orchestrator, at `dcde2c8b` (`:101–102`) | **open in both** — `OrderDetails.js:174–182` at `dcde2c8b` |
| 7 | `useScreenModel.js:80` (`pruned`), `helpers/DailySummary/selection.js` (`pruneSelection`), `totals.js` (`pruneSet`) | Three prune variants for one rule; each rebuilds the "present ids" set, and the model's wrapper adds the same-identity bail-out `pruneSelection` already has | A change to what counts as present (a composite id) must land thrice | One `pruneIds(ids, rows)` that returns the same Set when nothing dropped, for both the selection and the expansion set | orchestrator, at `dcde2c8b` (`selection.js:101`, `totals.js:73`, model `:56`) | **part, by #54's tail** (`a05d0220`): the product amounts are one-line `Figure`s now (`:153`), so ₹ cannot wrap; the cash line (`:170`) still passes `formatCurrency` into a wrapping `Line` |
| 8 | `components/RangeButtons.js:92` | The From / To buttons printed the **device** clock (`DD_MM_yy_HHmm` → `getDate` / `getHours`) while every row prints IST | A phone set to UTC shows a window whose times sit outside the rows' | — | agent | **part, by #53**: `DateTimeRange.js` deleted; `isoOf` still in `request.js:9` at both heads, now beside `helpers/DateRange`'s import-free parser and `pickers.js:30`'s copy |
| 9 | `components/RangeButtons.js:84` → `src/components/DatePicker/DTBS.js:494, 547, 561, 573, 589, 597, 473, 343` | The v2 screen mounted v1's DTBS, whose sheets render English-only literals — the AI.md strings rule, unguarded because ESLint's JSX-text ban stops at `src/{screens,components}/v2` | A Hindi user opened an English-only dialog | — | agent | **open in both** — `selection.js:101`, `totals.js:73`, model `:56` |
| 10 | `index.js:466` (the quick-return overlay) | `chromeBlock` mounted a second time, re-mounting DTBS's four `BottomSheetModal`s and four back-handler registrations on every overlay entry | Avoidable work on the scroll path of a low-RAM Android | — | agent | **open in both** for `DD_MM_yy_HHmm`'s local `two`; `formatRangePill`'s code gone in #53 (`47057bbc`; a comment mention lingered at #53, none at #54) |
| 11 | `helpers/DailySummary/range.js:211` (`sentTo`) | `to` had two meanings (a server-inclusive instant vs a picked minute) told apart by matching the millisecond value 23:59:59.999 | An Android pick of 23:59 sent 23:58:59.999 | — | agent | open at #53; **fixed in #54** (`f09d7b5b`: `HeaderRefresh` shared with the panel — `Panel.js` imports it at `dcde2c8b`) |
| 12 | `helpers/DailySummary/request.js:22` (`isoOf`), `DateTimeRange.js` (`dateOf`) | Three parsers of `Date` / ms / ISO string beside `range.js`'s `msOf` / `dateOf` | Drift | — | agent | **mostly gone in #53** (`47057bbc`): the sheet sits beside the list (`index.js:469`), out of the chrome block, so no modal is re-mounted; the block itself is still rendered twice at both heads (`{chromeBlock}` ×2), now holding the memo'd bar, the card and the control row |
| 13 | `src/utils/Dates/index.js:363` (`DD_MM_yy_HHmm`) | A local `two` padder beside `pad` in `src/utils/Currency`, and a second formatter for the shape `formatRangePill` already printed | Two prints of one shape, one device-local, one IST | — | agent | **gone in #53** (`47057bbc`): `range.js` deleted; the `[From, To)` rule (C‑1) replaced the special case |
| 14 | `components/Panel.js:114` | The panel's refresh control re-implemented `HeaderRefresh` line for line | Two copies of one control | — | agent | **gone in #53** (`47057bbc`): DTBS no longer mounted by the screen, the sheet's words in its own `{ en, hi }` table; at `dcde2c8b` nothing under `DailySummary/` names DTBS or DatePicker |
| 15 | `components/OrderDetails.js:70` | Product-line amounts and the cash line used `formatCurrency` (a breakable space after ₹) inside `flexWrap` rows, while `formatMoney` exists because ₹ and its digits broke apart on Android | A long product name at 1× on a 360 dp cover: ₹ on one line, the digits on the next | — | agent | **open in both** — `SummaryCard.js:101–102` at `dcde2c8b` |

## 2. What was checked and held

- **The cache key** is the body minus its cursor, so page 2 merges into page 1's entry and a new tab, range or chip set is a new entry; **the summary stays page 1's** (P‑11, A‑8) — both read in `daily_summary.js:50–76`.
- **The v3 tag sharing** with v1 (`order_msts_POST / LIST`) is what makes finding 1 reachable; the sharing itself is by decision D9.
- **The P‑6 prune on a refresh** is right for a page-1 fulfilment; finding 4 is only the load-more case past the cap.
- **`serializableCheck` is off** in the store, so a `Date` in query args is not a defect (the agent's own dropped candidate).
- **Suite:** 100 / 2,281 green at head; the three lint errors are the base's.

## 3. Where to fix

All three paging defects and the prune cap live in `daily_summary.js` and `useScreenModel.js`, both since reworked by #53 (`cursorFor`) and #54 (`useRefetchOnRefocus`, `useScrollToTopOnNewList`), so the fix belongs on the **top of the stack**, not here. Findings 1 and 2 share one design move — subscribe on page-1 args, fetch further pages unsubscribed — which also removes the `cursorFor` dance #53's review calls an altitude problem. The driver line is a one-liner anywhere.

## 4. Rounds (dated)

- **2026-09-23** — the user: "also code-review PR #52 and #51 use agent teams" → two agents at once (this and #51), the suite and lint at head in a scratch worktree, findings 1–7 re-read at `f44348fc` and `dcde2c8b`. "add PR #54 and #53 code review in vault" → the reviews folder; this note joined it. **Nothing applied to the code; nothing committed.**
- **2026-09-23, the stack re-checked** — the user: "#51 is oldest. we could have solved issues in #52, #53,#54. update all docs if issues resolved in any PR" → every finding's signature grepped at each later head (#52 `f44348fc`, #53 `44eb266e`, #54 `dcde2c8b`) and the last column rewritten as **Resolved?**, one entry per later PR, with the commit where one closed it. Counts in the verdict and §0 follow the column.
