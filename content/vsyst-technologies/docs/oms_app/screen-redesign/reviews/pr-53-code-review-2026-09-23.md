# Code review — app PR #53 (`app_v2_date_range_sheet` → `app_screen_daily_summary`)

> **What this is:** the review the user asked for on **2026-09-23** — "also code-review PR #53" · "add PR #54 and #53 code review in vault". PR: <https://github.com/ShikhaR-C/dzzlo_oms_app/pull/53> — *v2: a date-time range sheet and range bar (common component) — Daily Summary adopts them*. Its plan is [[09-date-range-sheet-plan]]; its body is [[designs/date-range-sheet/pr-53-body]]; the verdict on every rewritten or deleted test is [[designs/date-range-sheet/pr-test-verdicts-2026-09-20]].
> **Verdict:** **the design holds; six things to fix, four of them still live at the top of the stack** (`dcde2c8b` on `app_v2_cleanup`, 2026-09-23): a duration print that drops the minutes, the server's `meta.range` echo no longer read, the To picker's cap below a This-month boundary, and the stale remembered shift after the preload race. Two more are traps for the **next** screen that adopts the sheet (`showShiftTime={false}`, the bar's `anchor` default). No AI.md rule is broken: the deleted tests carry verdicts, colours and sizes are tokens, no font cap, no `Dimensions.get`, every string tabled in both languages.
> **Source:** head **`44eb266e`**, base `f44348fc` (`app_screen_daily_summary`, PR #52's head) — **14 commits, 58 files, +12,271 / −2,631**. Suite at head **112 suites / 2,778 tests green, 15.7 s** (run in a scratch worktree with `.env.ci`). ESLint: one error in the touched files, `src/navigation/AppNavigatorContainer.js` — `react-hooks/exhaustive-deps` on `dispatch` — which is **on the base at line 80**; this PR's ten inserted lines (`preloadDayStart()` at module level) only moved it to 90. Every `file:line` below is from `44eb266e`; the **Now** column says whether it is still there at `dcde2c8b`.
> **How:** the `code-review` skill at **high**, one forked agent over the whole diff plus the callers, the endpoint and the tests (43 tool calls, ~14 min); the orchestrator re-read findings 2–9 in the source at both `44eb266e` and `dcde2c8b` before writing them here (the **Checked** column), and read RTK Query 2.11.2's thunk `condition` for the in-flight rule PR #52's review leans on.
> **Companions:** [[09-date-range-sheet-plan]], [[screens/02-daily-summary]], [[08-v2-optimisation-plan]], [[reviews/pr-54-code-review-2026-09-23]] (the PR on top of this one — it moved the sheet into `Sheet.js` and split `DateRangeSheet.js`), [[reviews/pr-52-code-review-2026-09-23]] (the PR under it).

---

## 0. The verdict in one table

| | |
| --- | --- |
| **Defects, live now** | `formatDuration` prints "1 day 0 hrs" for a day-and-minutes window · the model no longer reads `meta.range`, so the bar can name a day the server did not list · `maxDateFor` caps the To picker a day past today while This month's boundary is next month's 1st · `apply` skips `rememberDayStart` when the committed window already carries the day start, so the stale stored shift survives the preload race |
| **Traps for the next adopter** | `showShiftTime={false}` edits a private copy with `dayStart: 0` but the model keeps the remembered 6 (Done does nothing; a real change clobbers the stored shift) · `DateRangeBar`'s `anchor` defaults to the live clock |
| **One-frame flaws** | the draft is re-copied from `applied` in an effect keyed on `[open]`, so the first frame after open shows the previous session's draft |
| **Fixed since, by #54** | the bar's triple formatting and unstable `onPress` (memo'd, keyed on `reworkWindow`); `barTextWidth` folded into the theme's one `textWidth`; the sheet frame in `Sheet.js` (the negative margin went with it, still open) |
| **Deliberate** | the backdrop's `pressBehavior="close"` + `onPress={dismiss}` — `Sheet.js` now says why |
| **Held** | the `[From, To)` rules, `combineIst` / `clampRange` at Tier 1, one request per Apply, the cursor stored with its window, `preloadDayStart` ahead of the auto-login read |

## 1. Findings, most severe first

| # | Where (`44eb266e`) | What | When it bites | Fix | Checked | Now (`dcde2c8b`) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `src/helpers/DateRange/format.js:153–162` (`formatDuration`) | `if (days > 0 && hours === 0 && minutes === 0) → days; if (days > 0) → daysHours(days, hours)`: a window of whole days **plus minutes only** falls into the second branch and prints `daysHours(1, 0)` | Custom 19 Sep 12:00 AM → 20 Sep 12:30 AM: `{ days: 1, hours: 0, minutes: 30 }` → "1 day 0 hrs selected" (Hindi "1 दिन 0 घंटे चुने गए"); the 30 minutes never print | A days + minutes sentence, or one days / hours / minutes sentence that drops zero parts | orchestrator, in source | **still there**, `format.js:166–171` |
| 2 | `src/screens/v2/Common/DailySummary/useScreenModel.js:124` | The removed `shown = query.range ?? currentData?.meta?.range ?? …` was the only reader of the server's echo. For Today under 12 - 12 AM the request sends **no range** and the server's clock decides the day, but the bar now prints `presetRange('today', 0, anchor)` from the phone's clock. `src/store/apis/v4/daily_summary.js:44–45` ("`range` is what the pills show after a request that sent none") is now false | A request at phone-time 23:59:58 IST, or a phone a minute slow: the server answers tomorrow's list with `meta.range` = tomorrow; the bar reads "Today · 19 Sep → 20 Sep" over 20 Sep's orders until the next refresh. Before this PR the pills printed the echo and could not disagree | Print `meta.range` when the request sent none, or always send the range and drop the "server decides" branch (and fix the endpoint comment either way) | orchestrator: no non-comment reader of `meta.range` in `src/` | **still there** |
| 3 | `src/components/v2/DateRangeSheet/pickers.js:94–100` (`maxDateFor`) with `window.js:439–444` (`maxPickDate`) | The To date picker's `maximumDate` is today's shift-day start + 1 day, but a **This month** window (and a Custom copied from it) has its printed boundary on the 1st of **next** month, so `pickerValueFor` hands the picker a value past its own maximum | 19 Sep, This month → Custom (fields 1/9 → 1/10), tap the To date: Android's `setMaxDate` moves the dialog to 20 Sep; OK without touching fires `set` with 20 Sep and `pickField('toDate')` moves the end from 30 Sep to 19 Sep 23:59:59.999. iOS clamps `date` to `maximumDate` the same way | Cap at `max(D0 + 1, the field's current boundary)`, or keep presets that end in the future inside the cap | orchestrator: `maxPickDate`, `pickerValueFor` and the month preset read; the platform clamp is the agent's | **still there**, `pickers.js:86–92` |
| 4 | `src/hooks/useDateRangeWindow.js:90–106` (`apply`) | `rememberDayStart` runs only when `window.dayStart !== committed.window.dayStart`. After the documented preload race (storage read lands after the screen mounted on 0 while the cache became 6), an apply of a 12 - 12 AM window sees `0 === 0` and never writes, so storage keeps 6 | Next screen and next app start open on 6 - 6 AM although the last applied window was 12 - 12 AM. `rememberDayStart` already no-ops on `cache === hour`, so the guard adds only this hole | Drop the guard; the test "an apply on the same shift writes nothing" then asserts against the cache, not the committed window | orchestrator, in source | **still there**, `useDateRangeWindow.js:80–83` |
| 5 | `src/components/v2/DateRangeSheet/DateRangeSheet.js:151–154, 204–208` | With `showShiftTime={false}` the sheet edits `applied = { ...value, dayStart: 0 }`, gates Done on `isChanged(draft, applied)` against that copy, and `apply()` on any real change calls `rememberDayStart(0)`. The model, meanwhile, still holds the remembered 6 | A date-only v2 screen on a phone that remembers 6: the request is 06:00 → 06:00, the sheet shows 12:00 AM → 12:00 AM, Done changes nothing; pick Yesterday and the dealer's stored shift is forgotten by a screen that never showed a shift card. **No screen passes `showShiftTime={false}` today** — this bites the next adopter | Move the override into the hook (`useDateRangeWindow({ shiftTime: false })`) so model, bar and sheet agree | orchestrator: mechanism read; no caller found in `src/screens` | **still there**, `DateRangeSheet.js:80` — latent |
| 6 | `src/components/v2/DateRangeSheet/DateRangeBar.js:42, 49`; `RangeSummary.js:149, 181` | `anchor = Date.now()` as a prop default, and the same number / Date coercion with a `Date.now()` fallback three times — the live clock the file headers say the bar must never read. Daily Summary passes `anchor={clock}`, so it is safe **there** | A second screen renders `<DateRangeBar window={…} onPress={…}/>` without `anchor` and nothing warns: at 6:00:00 AM under 6 - 6 AM a list-frame re-render prints tomorrow's shift day on the bar while the request holds today's — the §3.6 swap the model exists to prevent | Make `anchor` required (or fold `window` + `anchor` into one prop) and hoist the coercion into one helper | orchestrator, at `dcde2c8b` (`DateRangeBar.js:17, 25`; `RangeSummary.js:101, 119, 134`) | **still there** — latent |
| 7 | `src/components/v2/DateRangeSheet/DateRangeSheet.js:174–180` | The draft is re-copied from `applied` in a `useEffect` keyed on `[open]`, so the first committed frame after `open` flips true renders the **previous** session's draft — its window, its `openedAt`, an open Custom block — before the effect replaces it | Session 1: Custom, then backdrop. Session 2: tap the bar → `RangeSummary` (`accessibilityLiveRegion="polite"`) announces the abandoned custom window, `CustomFields` mount, `enableDynamicSizing` measures the taller content, then everything re-renders a frame later. "It opens filled with what is applied" is broken for one frame and one screen-reader announcement | Derive the initial draft at the transition: key the content on an open counter, or dispatch during render | orchestrator, in source | **still there**, `DateRangeSheet.js:99–102` |
| 8 | `src/hooks/__tests__/useDateRangeWindow.test.js:118–137` (also 161–167, 171–178) | `jest.spyOn(AsyncStorage, 'getItem').mockReturnValue(<never-resolving>)` is restored by `jest.restoreAllMocks()` at the **end of the test body**, while `afterEach` restores timers only | One failing `expect` at 134–136 throws before the restore, the pending mock leaks into every later test, `rememberSix()` → `preloadDayStart()` awaits forever, and a cascade of timeouts hides the one real failure | Move `jest.restoreAllMocks()` into `afterEach`, as `rememberedDayStart.test.js` and `AppNavigatorContainer.preload.test.js` already do | orchestrator, at `dcde2c8b` (`:80` afterEach, `:137` in-body restore) | **still there** |
| 9 | `src/components/v2/DateRangeSheet/DateRangeSheet.js:463` | `marginHorizontal: (width − READABLE_WIDTH) / 2` is negative for a `medium` window under 640 (the class starts at 600), so the hosting container is wider than the window while every fit rule was computed for `innerWidth(width)` | A 600 dp portrait tablet: margin −20, the sheet overhangs 20 pt each side, Close / Done and the radios clipped. A third copy of `CreditSheet` / `FilterSheet`'s expression | `Math.max(0, …)`, or `(width − contentWidthOf(width)) / 2` | orchestrator, at `dcde2c8b` | **moved to `Sheet.js:77`** by #54, listed there as a follow-up |
| 10 | `src/screens/v2/Common/DailySummary/useScreenModel.js:297` | Altitude: the "cursor stored with the window it was read under" dance (`cursorFor`, `windowRef`, the derived cursor) exists because the window lives in the hook and the cursor in the screen's query state, and `src/screens/v2/README.md` step 4 asks every adopting screen to re-implement it by hand | A future screen that forgets `cursorFor` ships the "new range with the old cursor" 400 the mutation smoke caught here | Own the cursor once — in `useDateRangeWindow` or a small `usePagedRequest` keyed on the request window | agent | **still there** (`cursorFor` at `:248–252`) |
| 11 | `DateRangeBar.js:56`, `index.js` (the bar's `onPress`) | Every bar render ran `rangeFor` + `formatRangeLine` three times, the bar was mounted twice on the phone, and `openSheet` was rebuilt every render on the whole `model` | Six formats per render for text that changes only on Done / rework | — | agent | **fixed by #54** (`c433a370`: bar and print memo'd, `onPress` on `reworkWindow`); the remnant is #54's finding 8 (`printsYear` twice) |
| 12 | `useScreenModel.js:318, 350` | `reworkRef` mirrors the already-stable `rework`; `reworkWindow` is an identity wrapper; `cursorRef` serves only `refresh()` | Three refs and a pass-through for values that never change identity | Return `rework` itself | agent | `reworkWindow` kept by #54 as the stable key the bar's `onPress` needs — acceptable |
| 13 | `layout.bar.js:226` (`barTextWidth`), `pickers.js:50` (`msOf`) | The measured-width formula written a third time; `window.js`'s private `msOf` re-implemented | Three copies drift the day one is re-measured | — | agent | **width fixed by #54** (one `textWidth` in `src/theme/scale.js`); `msOf` not checked |
| 14 | `DateRangeSheet.js:191` | Backdrop with `pressBehavior="close"` **and** `onPress={dismiss}` — two dismiss paths for one tap | Harmless while `onDismiss` is idempotent | — | orchestrator | **deliberate** — `Sheet.js:55` explains: `pressBehavior` makes a device tap work, `onPress` runs first |

## 2. What was checked and held

- **No leftover reader** of the removed exports and keys (`sentTo`, `clampPicked`, `formatRangePill`, the six string keys), and `meta.range` is read by no code — which is finding 2.
- **The window rules** (`[From, To)`, every end printing its own minute, the running shift under 6 - 6 AM, This month as the calendar month, `combineIst` / `clampRange` at Tier 1) match the plan's calls C‑1…C‑10 as quoted in [[09-date-range-sheet-plan]] §6a.
- **One request per Apply**, the 300 ms step debounce, `rework()` before a refresh and a re-focus, one sheet per screen with none in the quick return: each pinned in `DailySummaryRange.test.js` as the verdicts note records.
- **`preloadDayStart()` at module level** in `AppNavigatorContainer.js` is queued ahead of the auto-login read, as `AppNavigatorContainer.preload.test.js` pins; the lint error beside it is the base's.
- **Suite:** 112 / 2,778 green at head (the body says 112 / 2,738 after the first four commits; the later commits added 40).

## 3. Where to fix

PR #54 sits on this one and is not merged, so a fix here would need #54 rebased. The four live defects (1–4) and the test leak (8) are one small commit on the top of the stack; the two adopter traps (5, 6) and the altitude point (10) belong to the next screen's adoption work, or to a small follow-up on `useDateRangeWindow` before it. The endpoint comment (finding 2) should change in the same commit as whichever branch is taken.

## 4. Rounds (dated)

- **2026-09-23** — the user: "also code-review PR #53" → the skill run; the suite and lint at head in a scratch worktree; findings 1–9 and 14 re-read in the source at `44eb266e` and again at `dcde2c8b` to fill the **Now** column. "add PR #54 and #53 code review in vault" → this note. **Nothing applied to the code; nothing committed.**
