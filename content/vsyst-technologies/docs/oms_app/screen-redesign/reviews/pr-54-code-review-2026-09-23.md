# Code review — app PR #54 (`app_v2_cleanup` → `app_v2_date_range_sheet`)

> **What this is:** the review the user asked for on **2026-09-23** — "what do i have to review in this PR. the most important files" · "help me code-review these tier1 and tier2" · "add PR #54 and #53 code review in vault". PR: <https://github.com/ShikhaR-C/dzzlo_oms_app/pull/54> — *refactor(v2): clean-up waves 0–5 — test safety net, render fixes, the common kit, the big files split, comments and dead code, tidy (stacked on #53)*. Its plan is [[08-v2-optimisation-plan]].
> **Verdict:** **not mergeable as it stands — three small defects to fix, all in the 16 commits after wave 5, and the PR body is stale.** The waves 0–5 work the body describes held up: no correctness finding in the stripper, `textWidth`, the two shared hooks, the exits or the three big-file splits.
> **Source:** head **`5393b59f`** on `app_v2_cleanup`, base `44eb266e` (`app_v2_date_range_sheet`, PR #53's head) — **65 commits, 182 files, +13,932 / −9,752**. Suite at head **138 suites / 3,176 tests green, 16.4 s** (the body says 137 / 3,120 — the difference is the tail commits). ESLint clean on every source file the PR touches. Working tree clean. Every `file:line` below is from `5393b59f`.
> **How:** the `code-review` skill at **high**, one forked agent reading the whole non-test diff hunk by hunk and tracing every renamed or un-exported symbol (63 tool calls, ~16 min); the orchestrator then re-read six of its ten findings in the source before writing them here (the **Checked** column). The suite and lint were run separately by the orchestrator.
> **Companions:** [[08-v2-optimisation-plan]] (the waves), [[screens/01-customers]], [[screens/02-daily-summary]], [[09-date-range-sheet-plan]] (the sheet this PR splits), [[reviews/pr-53-code-review-2026-09-23]] (the PR under this one).

---

## 0. The verdict in one table

| | |
| --- | --- |
| **Defects** | 3 — a font-scale floor that shrinks figures past its documented minimum (`OrderDetails.js`), a Bold Text read that lands one frame late (`useBoldText.js` → `SummaryCard.js`), and a reset that PUTs the default prefs when only a search was typed (`Customers/useScreenModel.js`) |
| **Drift** | 2 — the sheet's picker rule measures the header with its own copy of the kit's two constants; `CreditAvatar.js` keeps a local `HAIRLINE = 1.5` in the PR that made `HAIRLINE = 1` a theme export |
| **Hygiene** | the body describes waves 0–5 (head `760ec167`); **16 commits sit on top of it**, four of them visible changes, none listed under *Visible changes* or *Device checks* |
| **Held** | the memo'd `CustomerRow` (FlashList 2.3.1 compares `renderItem` identity; the exits are stable), `monthName`'s 12-entry guard, the removed `it` blocks (Prettier re-wraps except the two the body records) |

## 1. What to review, ranked (what the user asked first)

**Tier 1 — where behaviour can change silently.** `src/test/sourceText.js` (the parser-based stripper every source-reading suite depends on) · `src/theme/scale.js` (`textWidth` takes the wider em for an unknown platform; every fitted number goes through it) · the Customers split: `Dealer/Customers/index.js`, `useScreenShape.js`, `useYieldLadder.js`, `useExits.js`, `components/CustomerList.js`, `components/CustomerRow.js` · the Daily Summary split: `Common/DailySummary/index.js`, `useCollapsingChrome.js`, `useRangeSheet.js`, `components/OrderList.js`, `layout.js` · the sheet split: `DateRangeSheet.js`, `useSheetShape.js`, `useFieldPickers.js`, `usePickerScroll.js` · the shared frame `components/v2/Sheet.js` + `SheetHeader.js` · `screens/v2/shared/useRefetchOnRefocus.js` + `useScrollToTopOnNewList.js`.

**Tier 2 — the tail after wave 5, which the body does not describe** (`760ec167..5393b59f`, 16 commits): `theme/provider/useBoldText.js` (new) + `SummaryCard.js` + `DailySummary/layout.js` (the 16 Sep split card back as the default, tiles under Bold Text) · `OrderDetails.js` (hairlines between expanded lines, Roboto Condensed figure columns) · `PanelColumn.js` (divider prop) · `DateRangeSheet/layout.bar.js` + `RangeSummary.js` + `helpers/DateRange/format.js` (calendar glyph, the wash gone, `printsYear`) · `Customers/useScreenModel.js` + `strings.js` + `strings.hi.js` (reset clears the search) · `helpers/DailySummary/chips.js` + `OrderRow.js` (Delivered / Invoiced glyph tone) · `DateRangeSheet.js` (the header moves into the scroll).

**Tier 3 — verify the claims, skim.** `f905b8b3` is the only commit that removes tests (8 cases from `DailySummary/__tests__/layout.test.js`; no test file is deleted) · `src/test/testUtils.js` (wave 5's shared helpers; "every test file keeps its exact `expect(` count") · the comment-pass and Prettier commits ("AST-identical", check mechanically) · `docs/testing.md`, `docs/screens/*.md`, `AI.md`.

Churn against the base (source, not tests): `Customers/index.js` 1,516 · `Customers/layout.js` 984 · `DailySummary/layout.js` 963 · `CreditSheet.js` 726 · `DailySummary/index.js` 648 · `DateRangeSheet.js` 615.

## 2. Findings, most severe first

| # | Where | What | When it bites | Fix | Checked |
| --- | --- | --- | --- | --- | --- |
| 1 | `src/screens/v2/Common/DailySummary/components/OrderDetails.js:52–54` | `Figure` sets `minimumFontScale={Math.min(DETAILS_MIN_FIGURE_SCALE, scale)}`, so the documented 0.6 floor (`layout.js:239`) gives way whenever the three fixed columns are squeezed, and the shrink is driven by the OS text size itself. AI.md: *"Font scaling is never capped … If a layout breaks at 200 %, the layout is wrong."* Commit `a05d0220` chose "squeezed never wrapped" on purpose, so this is a call to make explicit, not an accident | iOS AX‑L on a 320 pt list: the columns need ≈ 378 pt against ≈ 205 pt of room → `scale` ≈ 0.54, every qty / rate / amount draws at ~54 % of its scaled size; at AX‑XXXL ≈ 30 %. The user who enlarged text gets figures smaller than at 1× | Let the three figures stack under the name at the sizes where they no longer fit (as `amountOwnLine` does for the row) and keep `adjustsFontSizeToFit` for the value-past-promise case only — or record in the runbook why this case is exempt | orchestrator, in source |
| 2 | `src/theme/provider/useBoldText.js:12–27` → `SummaryCard.js:189–202` (`useArrangement`) | The hook starts at `false` and learns the real value only when `AccessibilityInfo.isBoldTextEnabled()` resolves. The card reads it on first render, so an iOS Bold Text user sees the `split` card paint and then reflow to tiles a frame later (the value-driven reshape decision P‑15 c forbids). Each card instance (two during the quick return) holds its own listener. Android is always `false`, so no reflow there | iOS, Bold Text on: open Daily Summary → `summary-split` paints, the promise resolves, the card becomes `grid` / `list`, the control row under it jumps | Read Bold Text once at the provider / window-class level, or key `Screen`'s subtree on it as it already does for `fontScale` and `language`, so the first render already knows | orchestrator, in source |
| 3 | `src/screens/v2/Dealer/Customers/useScreenModel.js:120–124` (`resetPrefs`) with `applyPrefs` at `:103–116` | Reset now runs for a typed search alone (`acfe5d27`) but still calls `applyPrefs(DEFAULT_PREFS)` unconditionally, which makes `sort` / `dir` / `filters` explicit in the query (a refetch of the same list) and fires `putPrefs` with the defaults even when no preference was applied | Type "ab", tap Reset → besides clearing the search the screen sends a second list request and a prefs PUT that changes nothing on the server | Only call `applyPrefs` when `badgeCount > 0`; clear `q` alone otherwise | orchestrator, in source |
| 4 | `src/components/v2/DateRangeSheet/layout.js:30, 51, 420` vs `src/components/v2/SheetHeader.js:21, 23` | `sheetChrome` (and through it `datePickerDisplay` → calendar vs wheel) measures the header with its own `HEADER_ROLE` / `HEADER_PAD_V`, while the header is now drawn by the kit's `SheetHeader` from `SHEET_TITLE_ROLE` / `SHEET_HEADER_PAD_V`. Both say `titleMedium` / 10 today | Someone tunes `SHEET_HEADER_PAD_V`; the picker rule keeps budgeting 10 pt and the iOS calendar is offered where its last row now sits under the edge (or the wheel where there was room) | Import the two constants from `../SheetHeader` in `layout.js` and delete the local pair | orchestrator, in source |
| 5 | `src/screens/v2/Dealer/Customers/components/CreditAvatar.js:23, 98` vs `src/theme/layout.js:32` | A local `const HAIRLINE = 1.5` (the dotted UNLIMITED ring's stroke, scaled by `fontScale`) shadows the theme's `HAIRLINE = 1`, lifted in this same PR and imported by every sibling | A later edit imports `HAIRLINE` from the theme here and the ring's stroke silently becomes 1 pt, or the reverse | Rename to `RING_STROKE` (or `UNLIMITED_STROKE`) | orchestrator, in source |
| 6 | commit `acfe5d27` (`Customers/index.js`, `useScreenModel.js`, `strings.js`, `strings.hi.js`) | The header Reset now shows for any typed search and clears it, with new copy "Reset search, filters and sort?" (en + hi). A product change absent from the body's *Visible changes*, which says those are the only visible changes | A reviewer approving against the body ships an unreviewed behaviour and copy change inside a refactor PR; a dealer who typed a name sees a Reset glyph that used to mean "filters applied" | Add it to the body and the Customers runbook, or move it to its own PR | orchestrator, in the log |
| 7 | `src/components/v2/Sheet.js:75–78` | `marginHorizontal: (width − READABLE_WIDTH) / 2` goes negative on a `medium` window narrower than 640 pt (600–639), so the frame widens past both edges instead of containing. Moved into the kit unchanged; the body lists it as a follow-up | A 620 pt window (iPad Split View's narrow half, some foldable widths): margin −10, all three sheets overflow each side by 10 pt | `Math.max(0, …)`, or `undefined` while `width < READABLE_WIDTH` | orchestrator, in source; known |
| 8 | `src/helpers/DateRange/format.js:130`, `RangeSummary.js:155`, `helpers/DailySummary/format.js:20–22` | `formatRangeLine` has `openParts` / `endParts` in hand but calls `printsYear`, which re-reads both plus `now` through `istParts`; `RangeSummary` calls `printsYear` again for the same range; `formatOnDt` reads `istParts(instant)` twice per row | Six IST arithmetics per bar render where two would do, two per list row — in a PR whose stated aim is fewer renders. Not a crash | Return the `printsYear` answer from `formatRangeLine` (or accept the parts); let `formatOnDt` pass the parts it has into `formatDate` | agent only |
| 9 | `CustomerRow.js:293` (`RowOnItsOwn`, the `inPane` prop) and `CreditAvatar.js:149` (`AvatarOnItsOwn`) | Production branches that exist only so the component suites can mount without a `shape` / `fontScale`; the screen always passes them. The pane / content-column width logic is now written twice (`useScreenShape` and `RowOnItsOwn`) | A drift in `RowOnItsOwn` is invisible in the app while the suite passes | Have the suites build a `shape` (via `useRowShape` or a fixture) and delete the fallbacks | agent only |
| 10 | `SummaryCard.js:192` (the `stacked` prop, `typeof width !== 'number' && stacked`) | Survives only for the "screen's old flag, without a width" test; the screen always passes `width` and never `stacked` | A future caller passes `stacked` expecting an effect it no longer has once a width is present | Drop the prop; let `summaryArrangement`'s no-width branch be the only fallback | agent only |

## 3. What was checked and held

- **`CustomerRow` memo without `extraData`** is safe: FlashList 2.3.1's `ViewHolder` compares `renderItem` identity, and the four exits from `useExits` are stable for as long as `navigation` stands.
- **`monthName`** keeps the old 12-entry guard after the one-resolver change (`dd926e4c`).
- **Removed `it` blocks** (33 in the diff): Prettier re-wraps, except the `cardStacks` dead-rule tests (`f905b8b3`) and the `CustomHeader.js` source read that became a render check (`9466ea07`) — both recorded in the body.
- **The Delivered / Invoiced tone flip** (`42af2baa`) and **reset-clears-search** (`acfe5d27`) each landed as their own red → green pair with docs.
- **Tier 1 produced no correctness finding**: `sourceText.js` (parse or throw, never unstripped; JSX comments cut with their braces), `textWidth`'s `em[os] ?? Math.max(em.ios, em.android)`, `useRefetchOnRefocus` (skips the first focus by design, empty deps on purpose), `useScrollToTopOnNewList`, `useExits`, the three splits.
- **The one ESLint error in the tree** — `src/navigation/AppNavigatorContainer.js:90`, `react-hooks/exhaustive-deps` on `dispatch` — is not this PR's: it is on `app_screen_daily_summary` at line 80 and PR #53's ten inserted lines only moved it (see [[reviews/pr-53-code-review-2026-09-23]]).

## 4. The 16 commits the body does not describe (`760ec167..5393b59f`)

`d861af86` / `a05d0220` / `706b93ed` — hairline above each expanded line, `productFigures` (finding 1), Roboto Condensed values but the remarks, `DETAILS_FIGURE_EM` · `f245779b` / `42af2baa` — Delivered and Invoiced glyphs in primary (accent in dark) · `413bab23` / `0fe0f9c4` — `PanelColumn` divider prop, Daily Summary opts in · `9559935c` / `acfe5d27` — the header reset clears the search (finding 6) · `18b9441d` / `88dfd1ae` — the sheet header rides in the scroll as its sticky first child · `8d194ff0` / `a1e8cc28` — calendar glyph at the bar's left, the wash gone, `printsYear` · `2c6eeccd` / `80a9cb20` / `5393b59f` — the 16 Sep split card back as the default, `useBoldText` (finding 2).

Before merge the body's *Visible changes*, *Device checks* and *Tests* sections need these; four of them are visible on the device.

## 5. Rounds (dated)

- **2026-09-23** — the user: "what do i have to review in this PR. the most important files" → the tiers in §1 (the stale body noticed here). "help me code-review these tier1 and tier2" → the skill run, the suite (138 / 3,176, 16.4 s) and lint by the orchestrator, findings 1–6 re-read in source. "add PR #54 and #53 code review in vault" → this note. **Nothing applied to the code; nothing committed.**
