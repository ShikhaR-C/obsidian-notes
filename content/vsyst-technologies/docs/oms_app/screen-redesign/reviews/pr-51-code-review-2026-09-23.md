# Code review — app PR #51 (`app_screen_customers_design` → `app_screen_customers`)

> **What this is:** the review the user asked for on **2026-09-23** — "also code-review PR #52 and #51 use agent teams". PR: <https://github.com/ShikhaR-C/dzzlo_oms_app/pull/51> — *Customers v2: design restyle (screen 01, stacked on #50)*. Its spec is [[screens/01-customers]] with [[01b-customers-filter-sheet]]; the foundations it landed (the type scale F‑APP‑7, window classes and orientation F‑APP‑9, language F‑APP‑10) are in [[02-foundations]].
> **Verdict:** **the foundations hold; four defects are still live at the top of the stack** (`dcde2c8b`, 2026-09-23): the UNLIMITED credit bar is the wrong width after a rotation because v1's `ProgressSVG` froze the window width at launch; every box in the Customers layout is measured with the Latin line height while Hindi draws 1.2× taller; the sort button announces its raw key to a screen reader; and the two sheets' open state lives above the `Screen` remount key, so a text-size change strands it. Two need a device to settle: the keyboard paid twice on Android ≤ 14 under `adjustResize`, and Android 13+'s per-app language picker not honoured until the process restarts. **Nothing was fixed by #52 or #53. Three findings are fixed by #54** (stable rows and exits, one `Sheet`, the stale comment) **and two reduced** (the per-row keyboard listeners; the negative margin now in one place). Nine of fourteen are open at `dcde2c8b`.
> **Source:** head **`d0fbd081`**, base `bf18c2e8` (`app_screen_customers`, PR #50's head) — **140 commits, 126 files, +25,449 / −1,151**. Suite at head **71 suites / 1,588 tests green, 15.1 s** (scratch worktree, `.env.ci`). ESLint over the touched files: one error, `src/navigation/AppNavigatorContainer.js:80` (`react-hooks/exhaustive-deps` on `dispatch`), **on the base at line 79**; the rest are v1's `no-unstable-nested-components` warnings and three unused icon imports. Every `file:line` below is from `d0fbd081`; **Now** says whether it is still there at `dcde2c8b`.
> **How:** the `code-review` skill at **high**, one forked agent over the whole diff (48 tool calls, ~30 min; it confirmed the native stacks are pinned, the "deleted" tests were rewritten with verdicts, and `tint()`'s callers pass only hex or the intended rgba tokens); the orchestrator re-read findings 1, 3, 4, 5, 7–11 and 13 at `dcde2c8b` (the **Checked** column).
> **Companions:** [[screens/01-customers]], [[02-foundations]], [[08-v2-optimisation-plan]] (whose RT‑1 / CK‑14 closed findings 12 and 14), [[reviews/pr-52-code-review-2026-09-23]] (on top of this), [[reviews/pr-53-code-review-2026-09-23]], [[reviews/pr-54-code-review-2026-09-23]].

---

## 0. The verdict in one table

| | |
| --- | --- |
| **Defects, live now** | `ProgressSVG`'s module-scope `Dimensions.get('window')` under a rotating screen (`CreditSheet.js:187`, the bar; `CreditAvatar.js` too) · `lineHeightAt(…, language)` never given the language in `Customers/layout.js` (`:293`) · `accessibilityLabel={sort}` (`SortMenu.js:151`) · `filtersOpen` / `creditRow` above the `Screen` key (`index.js:1410`) · the Settings Language row's literal `SYSTEM` / `ENGLISH` (`Settings/index.js:169`) |
| **Needs a device** | `paddingBottom` + `keyboardInset` under `adjustResize` on Android ≤ 14 (`index.js:879`) · `I18nManager`'s locale read once per process while the manifest advertises `localeConfig` (`deviceLocale.js:35`) · `AppText`'s Hindi rounding before the OS multiplier vs `lineHeightAt`'s after (`AppText.js:62`) |
| **Cleanup, live now** | the drawer `screenOptions` block identical in `Dealer/Drawer.js` and `Customer/Drawer.js` · `useWindowClass` still bundles the keyboard listeners for callers that read only width |
| **Fixed by #54 — nothing by #52 or #53** | `renderItem` and the four exits stable (RT‑1, `6261cbbd`) · one `Sheet` for both sheets (CK‑14, `c0ded9cb`; the negative margin moved with it, still open) · the stale `maintainVisibleContentPosition` paragraph gone. **Reduced by #54:** the row no longer calls `useWindowClass` in the app path (only the test-only `RowOnItsOwn` does) |
| **Held** | the native stacks pinned portrait, v2 opting in through the registry; the rewritten tests carry verdicts; colours through `tint()` and tokens; the type scale's "text sets the box" on the row and the chrome; Hindi through `useStrings` on the v2 screen |

## 1. Findings, most severe first

| # | Where (`d0fbd081`) | What | When it bites | Fix | Checked | Resolved? (#52 `f44348fc` · #53 `44eb266e` · #54 `dcde2c8b`) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `src/screens/v2/Dealer/Customers/components/CreditSheet.js:187` (`barWidthCent`) with `src/components/SVG/progressBar/index.js:6` | `barWidthCent(width)` divides by the live window width, but `ProgressSVG` multiplies the fraction by a module-scope `Dimensions.get('window').width` frozen at launch — AI.md's own "never `Dimensions.get` at module level" — and this screen rotates. The comment says the hook's width "is what makes the bar follow a rotation"; it is the opposite | Launch upright on 390, rotate to 844, open an unlimited customer: bar = 390 × 0.69 = 269 pt in a 582 pt card. Launch on the side, rotate upright: 844 × 0.85 = 718 pt, out through the 332 pt card | Give `ProgressSVG` a `width` prop (or read `useWindowDimensions` inside it) — it has ten v1 callers, so the prop is the safe route; `CreditAvatar` uses it too | orchestrator: `progressBar/index.js:6`, `CreditSheet.js:70, 279` at `dcde2c8b` | **open in all three** — `progressBar/index.js:6` untouched by the stack; `CreditSheet.js:70, 279` at `dcde2c8b` |
| 2 | `src/screens/v2/Dealer/Customers/layout.js:293` and every box (`tileHeight`, `nameRowMinHeight`, `tagLineHeight`, `captionLine`, `balanceLine`, `chipBoxHeight`, `searchRowHeight`, `cardHeight`, `rowHeight`, `columnsAlign`) | `lineHeightAt(type, role, fontScale, language = 'en')` is called without the language, while `AppText` draws Hindi 1.2× taller (`SCRIPT_LINE_HEIGHT.hi`). The row's minHeights, the `columnsAlign` equation and the yield ladder's chrome / row budget are Latin numbers under Devanagari | Hindi at 1×: the name renders 29 tall and each tile 36 against 24 / 32 minHeights, the text column ends ~9 pt below the avatar column — the defect `columnsAlign` exists to prevent; `chromeHeight` under-counts ~20 % and `yieldLevel` climbs a rung late | Thread `language` through the layout functions from `useTypeScale` / `useLanguage`, as `DateRangeSheet/layout.js` already does | orchestrator: three `lineHeightAt(` calls at `dcde2c8b`, none passing a language; the arithmetic is the agent's | **open in all three** — three `lineHeightAt(` calls, none with a language, at every head |
| 3 | `src/screens/v2/Dealer/Customers/components/SortMenu.js:151` | `accessibilityLabel={sort}` — the raw key (`createdAt`, `latestOrder`), not `s.filters.sort[sort]` | VoiceOver / TalkBack reads "createdAt, button" in either language; decision 69's "every word off `useStrings`" broken for the one control that opens the ordering | `accessibilityLabel={s.filters.sort[sort]}` | orchestrator, at `dcde2c8b` (`:92`) | **open in all three** — `SortMenu.js:92` at `dcde2c8b` |
| 4 | `src/screens/v2/Dealer/Customers/index.js:1410` | `Screen` remounts its subtree on a text-size or language change (`key={`fs:${fontScale}:${language}`}`), which unmounts both `BottomSheetModal`s, but `filtersOpen` / `creditRow` live above the key and are cleared only by `onDismiss`, which never fires for an unmounted modal | Filter sheet open → change the OS text size: the sheet vanishes, `filtersOpen` stays true, the fresh `FilterSheet` mounts its content invisibly, the old back-handler subscription is gone while the state says open | Reset the two flags when the key changes (an effect on `fontScale` / `language` in `useExits`, or move the flags under the key) | orchestrator, at `dcde2c8b`: `useExits(navigation)` at `index.js:75`, `<Screen` at `:214`, the key at `Screen.js:63` | **open in all three** — the flags moved from `index.js` into `useExits.js` in #54 (`4f583d95`), still with no reset on the key |
| 5 | `src/screens/Common/Settings/index.js:169` | The new Language row hard-codes `<Text>Language</Text>` and `LANGUAGE_OPTIONS` labels `SYSTEM` / `ENGLISH` — for the one row whose purpose is the reader who does not read English. AI.md: *"never a literal in JSX, never Hindi 'later'"*; the ESLint guard stops at `src/{screens,components}/v2` | A Hindi-only user on an English phone lands on "Language · SYSTEM · ENGLISH" | A `{ en, hi }` table for the row, read through `useStrings` | orchestrator, at `dcde2c8b` (`:45–46`) | **open in all three** — `Settings/index.js:45–46` |
| 6 | `src/screens/v2/Dealer/Customers/index.js:879` (the list's `paddingBottom`) | `keyboardInset` is added on both platforms, but the manifest keeps `windowSoftInputMode="adjustResize"`: where edge-to-edge is not enforced (Android ≤ 14, the Vivo-class devices in the device log) the platform already shrinks the root by the keyboard height, so the keyboard is paid twice | Android 13, tap search: the root resizes to 553 **and** the list gets 291 pt of extra bottom padding — a keyboard-high blank band under the last customer. Same for the panel's `ScrollView` | Pay the inset only where the root does not resize (iOS, or Android 15+ edge-to-edge) — one Platform / API-level branch in `useKeyboardInset` | orchestrator: the manifest (`:22`) and `useScreenShape.js:96` at `dcde2c8b`; the double count itself is the agent's — **run on an Android ≤ 14 device** | **open in all three** — the sum moved into `useScreenShape.js:96` in #54 (`4f583d95`), unchanged |
| 7 | `src/i18n/deviceLocale.js:35` with `AndroidManifest.xml` (`android:localeConfig`) | The manifest puts Dzzlo in Android 13+'s per-app language picker, but `I18nManager.getConstants().localeIdentifier` is read once per JS context, `locale` is not in `configChanges` (the activity recreates, the JS context survives), and `LanguageProvider` memoises on `[setting]` | Settings › App languages › Dzzlo OMS › हिन्दी: the activity recreates, `getDeviceLocale()` still says `en_IN`, the app stays English and the Settings row still reads SYSTEM | Re-read the locale on `AppState` foreground (or on the activity's recreate) and feed it into the provider | orchestrator: the reader (`:36`) and the manifest (`:11`) at `dcde2c8b`; RN's caching is the agent's — **run on an Android 13+ device** | **open in all three** — `deviceLocale.js:36`; `localeConfig` in the manifest |
| 8 | `src/components/v2/AppText.js:62` vs `src/theme/scale.js` (`lineHeightAt`) | The Hindi line is rounded **before** the OS multiplier in `AppText` (`ceil(lineHeight × 1.2)`, then RN scales) but **after** it in `lineHeightAt` (`ceil(lineHeight × fontScale × 1.2)`), so the drawn line can exceed the box by up to a point — the "half a point short" clipped descender `scale.js` warns about | `labelMedium` (16) in Hindi at 2.0: `AppText` draws 40, `boxHeight('labelMedium', 6)` = 39 + 12 → the chip's minHeight is 1 pt under its own text; `captionTiny` 30 vs 29 | Derive `AppText`'s number from the same `lineHeightAt(typography, variant, 1, language)` — one rounding path | agent | **open in all three** — `AppText.js` still rounds on its own path; the size of the gap is the agent's arithmetic |
| 9 | `src/theme/provider/useWindowClass.js:71` | `useWindowClass()` unconditionally mounts `useKeyboardInset()` (two Android or four iOS `Keyboard.addListener`s plus state), and at `d0fbd081` it was called per list row (`CustomerRow`), in `SummaryStrip`, `SortMenu`, `Container`, both sheets and the screen, although most read only `width` | A keyboard show / hide fired ~60–80 listeners and a re-render in every mounted row, four bursts per open on iOS | Keep `useWindowClass` a pure `useWindowDimensions` derivation; read the keyboard once, where `availableHeight` is consumed | orchestrator, at `dcde2c8b`: `useWindowClass.js:28` still mounts it; `CustomerRow.js:295` calls it only inside the test-only `RowOnItsOwn` (see #54's finding 9); `SummaryStrip`, `SortMenu`, `Container` still do | **hook unchanged in all three**; the per-row cost gone in #54 (`6261cbbd`, RT‑1) — the row's one call survives only in the test-only `RowOnItsOwn`; `SummaryStrip`, `SortMenu` and `Container` still call it |
| 10 | `src/navigation/Dealer/Drawer.js:73` = `src/navigation/Customer/Drawer.js:73` | The 17-line `screenOptions` object and its 20-line comment are identical in both drawers, and rebuilt on every render | The next drawer tweak lands twice or the roles drift | `drawerScreenOptions(width, height)` in `src/navigation/Common`, `useMemo`'d at the two call sites | orchestrator: the two blocks diff identical at `dcde2c8b` | **open in all three** — the two blocks diff identical at every head |
| 11 | `src/screens/v2/Dealer/Customers/components/CreditSheet.js:470`, `FilterSheet.js:102` | `marginHorizontal = (width − READABLE_WIDTH) / 2` negative for 600 ≤ width < 640 | A 600 dp tablet: the sheet 640 wide, 20 pt clipped each side | `Math.max(0, …)` | orchestrator | **open, in one place since #54** — five files at #52 and #53 (`CreditSheet`, `FilterSheet`, `DateTimeRange` / `DateRangeSheet`, two tests), one at #54: `Sheet.js:77` (`c0ded9cb`), listed there as a follow-up |
| 12 | `CreditSheet.js` / `FilterSheet.js:75` | `FilterSheet` duplicated `CreditSheet`'s whole sheet scaffold, the negative margin copied with it | Two sheets wearing one behaviour | — | agent | open at #52 and #53; **fixed in #54** (`c0ded9cb`: both sheets draw in `Sheet` under `SheetHeader`) |
| 13 | `src/screens/v2/Dealer/Customers/useScreenModel.js:97` | Stale: "`maintainVisibleContentPosition` stays on" while `index.js:1269` turns it off | Two contradictory accounts | — | orchestrator: no such paragraph at `dcde2c8b`; `CustomerList.js:145` has `{ disabled: true }` | open at #52 and #53; **fixed in #54** (`1356f357`) — no such paragraph in `useScreenModel.js`; `CustomerList.js:145` has `{ disabled: true }` |
| 14 | `src/screens/v2/Dealer/Customers/index.js:883` | `renderItem` and the four exit handlers recreated on every render of a screen that now re-renders on every keyboard event, `onLayout` and keystroke; FlashList re-rendered every visible row each time | Typing "Sha": three keystrokes + four keyboard events + the `onLayout`s each re-ran every row | — | agent | open at #52 and #53 (no `useCallback` on `renderItem` at either head); **fixed in #54** (`6261cbbd`: the row memo'd, `useExits` stable, `renderItem` stable — a keystroke redraws 0 rows) |

## 2. What was checked and held

- **Orientation:** every v1 native stack stays `'portrait'`; v2 opts in through `screenOptionsFor` in the registry (F‑APP‑9), and "rotating screens ask for `'default'`, not `'all'`" landed in #52.
- **Tests:** the suites the PR rewrote carry verdicts in its body; none was deleted without one.
- **Colours:** `tint()`'s callers pass hex or the intended rgba tokens; no literal reached a v2 component.
- **The type scale** (`scale.js`, `useTypeScale`, the `fontScale` harness) is the mechanism every later screen builds on; finding 2 is a caller's omission, not the scale's.
- **Suite:** 71 / 1,588 green at head; the lint error is the base's.

## 3. Where to fix

Findings 1–5 are small and belong on the top of the stack (`ProgressSVG` needs a prop because ten v1 screens share it). Findings 6 and 7 need a device run before code: an Android ≤ 14 phone for the keyboard band, an Android 13+ phone for the per-app language picker. Finding 8 is one function in `src/theme/scale.js`. Findings 9 and 10 are plan‑08‑style cleanup.

## 4. Rounds (dated)

- **2026-09-23** — the user: "also code-review PR #52 and #51 use agent teams" → two agents at once; the suite and lint at head in a scratch worktree; ten findings re-read at `dcde2c8b`. "add PR #54 and #53 code review in vault" → the reviews folder; this note joined it. **Nothing applied to the code; nothing committed.**
- **2026-09-23, the stack re-checked** — the user: "#51 is oldest. we could have solved issues in #52, #53,#54. update all docs if issues resolved in any PR" → every finding's signature grepped at each later head (#52 `f44348fc`, #53 `44eb266e`, #54 `dcde2c8b`) and the last column rewritten as **Resolved?**, one entry per later PR, with the commit where one closed it. Counts in the verdict and §0 follow the column.
